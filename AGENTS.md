# 量化交易项目｜因子挖掘·投研·回测·实盘 全场景生产级 AGENTS.md

> **适用场景**：量化因子挖掘、多因子投研、策略开发、策略迭代回测、自动化实盘交易、风控治理、AI量化智能体开发（覆盖股票/期货/ETF/加密货币全品类）
>
> **适配工具**：Cursor、Aider、Windsurf、GitHub Copilot、Claude Code（CLAUDE.md 导入生效）
>
> **核心原则（四场景统一）**：因子可解释优先（黑箱因子须经统计验证）、回测可复现、实盘可落地、零未来函数、零数据泄露、零数据窥探、统计严谨（多重检验）、全链路可追溯、风控压倒一切。

## 一、项目基础规范

- **技术栈**：Python 3.11+, Pandas, NumPy, SciPy, Statsmodels, TA-Lib, Numba, DuckDB, Pydantic V2, 可选：LangGraph（交易智能体）
- **Python路径**：'C:\Program Files\Python312\python.exe'
- **K线主路径**（统一路由引擎，含熔断器+缓存+降级）：`DUCKDB_CACHE`（本地缓存 Top1）→ `TDX_LOCAL`（通达信本地 HTTP 统一源 127.0.0.1:17709，日线+分钟+快照，v2.87.0 起合并 TQLocal/TDXMinute，TQ_LOCAL 旧 7721 已废弃）→ `TQ_PYTHON`（TQ-Python SDK）→ `AKSHARE`（即时获取降级）→ `SYNTHETIC`（合成数据兜底保证可运行）；分钟级降级链 `minute_cache → TDX_LOCAL`（17709）→ `TQSDK`（天勤，分钟/日线）；tick 降级链 `tick_cache → TQSDK_TICK`；实时价路径 `TDX_LOCAL → AKSHARE`；`WIND`/`IFIND` 仅用于字段增强层（宏观/基本面），多源交叉验证优先（≥2 源同日数据差异 >0.5% 记录分歧）
- **数据持久化（六层存储架构，plans/29，SSOT 单一事实源）**：L1 配置层（`seeds/*.yaml` + `config/`，YAML 文件）→ L2 行情库（`data/fts_history.duckdb`，kline/minute/tick，追加式 + 按年冷热归档至 `data/archive/*.parquet`）→ L3 因子资产库（`data/factor_catalog_{stock,futures}.duckdb`，因子/评估/版本/审查/血缘，**DuckDB 为权威存储**）→ L4 运行状态库（`data/state.db` **SQLite WAL**，`state_kv` 当前状态表 + `state_history` 历史追加表可回放，StateKVStore；E.3 S2 2026-08-13 自 DuckDB 迁移，多读单写不互斥）→ L5 信号缓存（Parquet 列式 + checksum，`memory/cache/factor_signals/`）→ L6 日志血缘（JSONL 保留追加 + 摘要入库）。**核心约束**：结构化数据统一进 DuckDB/SQLite 且一数一源，禁止同类数据双写漂移；JSON 一律降级为只读快照/兼容回退（旧格式冻结期后退役清理）；信号/大面板与归档层走 Parquet；`fts/store/` 数据访问层（`StorageRegistry` + `storage_landscape.yaml` 域登记）为唯一读写入口契约，新增数据域必须先登记后落库。**DuckDB 连接生命周期（E.4 S1 2026-08-13）**：L2/L3 写连接一律短生命周期（`_write_scope` = `fts/store/duckdb_lock.py` 跨进程 filelock 互斥 + 写操作完成即关，秒级），读连接一律 `read_only=True` 短连接；禁止模块级常驻写连接（`_WRITER`/`_DB`/`_cache_conn` 已移除）；跨进程写经 `data/.locks/*.duckdb.lock` 串行化；repository `lock_configuration=true` 兼容读写共存。
- **四大核心场景**：因子挖掘（离线）、策略开发与回测（仿真）、自动化实盘（生产）、风控治理（贯穿），场景代码完全解耦、逻辑严格对齐
- **环境隔离**：研发环境、回测环境、实盘环境配置独立，禁止环境参数混用
- **通用开发准则**：向量化优先、参数配置化、逻辑极简、分层解耦、兼容离线与生产双模式

## 二、标准化目录结构（适配四场景）

严格区分因子挖掘、回测验证、实盘交易模块，禁止跨场景乱引用，保证链路清晰。

```text
src/
├── research/        # 【场景1：因子挖掘｜离线专属】
│   ├── data/        # 行情接入、清洗、复权、对齐（多源交叉验证）
│   ├── factors/     # 量价因子、统计因子、舆情因子、复合因子
│   ├── labeling/    # 样本标签、前向收益、涨跌标签构建
│   ├── analysis/    # 因子IC、IR、单调性、稳定性、换手率分析
│   └── visualize/   # 因子可视化、分布绘图、收益曲线绘图
├── backtest/        # 【场景2：策略回测｜仿真专属】
│   ├── engine/      # 时序回测引擎、事件驱动模拟
│   ├── simulation/  # 滑点、手续费、涨跌停、停牌、容量仿真
│   ├── metrics/     # 量化评价指标体系
│   └── report/      # 回测报告自动生成
├── live_trade/      # 【场景3：实盘交易｜生产专属】
│   ├── gateway/     # 交易所/券商接口、行情推送、订单通道
│   ├── strategy/    # 实盘信号、调仓逻辑、时序对齐
│   ├── risk_control/# 实时风控、仓位限额、止损止盈、单日风控
│   └── monitor/     # 实盘监控、日志告警、异常探测
├── common/          # 全场景公共能力
│   ├── data/        # 行情加载、数据清洗、周期重采样
│   ├── logger/      # 分级日志、全链路日志记录
│   ├── utils/       # 通用工具、数值计算、时间处理
│   └── exception/   # 自定义业务异常
├── config/          # 场景化配置（research/backtest/live 独立配置）
└── agent/           # 可选：LangGraph 交易智能体流程编排
tests/               # 分场景单元测试：因子测试、回测测试、实盘逻辑测试
scripts/             # 离线挖掘脚本、批量回测脚本、实盘启动脚本
docs/                # 项目文档、设计决策记录、FAQ
```

## 三、全局编码通用规范

- 全代码强制类型注解，入参、返回值、复杂数据结构必须显式标注。
- 所有业务参数、阈值、约束全部使用 Pydantic V2 配置模型管理，禁止硬编码。
- 禁用原生 print，统一使用分级日志，区分研发调试、回测记录、实盘告警日志级别。
- 数值计算强制兜底：处理 NaN、Inf、零值、极端行情，杜绝策略报错、卡死、异常失效。
- 时间与序列统一使用 `datetime64`/`Timestamp` 类型，禁止字符串比较排序、禁止隐式时区错配。
- 文件小写下划线、类名大驼峰、函数语义化命名，核心代码必须补充完整 docstring。
- 量化计算优先向量化，禁止低效循环，高频实盘逻辑支持轻量化、低延迟适配。
- 依赖管理：使用 requirements.txt 或 pyproject.toml 锁定核心依赖版本，定期更新并测试兼容性。
- 代码审查清单：提交PR前，开发者应自查是否违反上述规范，特别是未来函数、数据窥探和硬编码问题。

## 四、分场景强制红线规则（核心约束）

### 4.1 因子挖掘场景红线（离线）

- 绝对禁止未来函数：因子计算仅使用当前及历史数据，禁止使用当期未闭合K线、未来行情数据。
- 严格数据时间切割：训练集、验证集、测试集按时间严格分割，禁止数据泄露、样本复用。
- 零数据窥探：禁止用全样本统计量选择因子或定参，因子筛选与验证必须使用不同时间窗。
- 多重检验校正：批量因子挖掘必须做多重检验校正（Bonferroni/FDR），防止大量因子中"碰巧有效"的幸存者偏差。
- 因子可解释性优先（非硬性否决）：优先采用可解释、可拆解、可溯源的因子；确需使用黑箱/降维复杂因子（如 PCA、ML 集成等）时允许纳入，但必须满足：① 通过统计严谨性验证（IC/样本外/多重检验校正）；② 登记血缘与变换链路（输入因子 → 变换 → 输出可追溯）；③ 效果显著优于可解释替代方案或填补可解释方案无法覆盖的能力缺口。评审时解释性降级需在备注中说明理由。
- 因子去冗余：因子入库前必须做相关性筛查，共线因子只保留代表，避免共线性污染组合。
- 固定随机种子：抽样、降噪、模型训练全固定种子，保证因子结果可复现。
- 单因子职责单一：一个因子函数仅实现单一逻辑，复合因子由基础因子组合生成。
- 因子存储规范：挖掘完成的因子统一入库 `factor_catalog_*.duckdb`（L3 因子资产库，SSOT，附带计算日期、参数、版本号等元数据），JSON 仅作只读快照备份；禁止直接落散文件（.json/.npy 等）绕过存储层，信号/大面板类数据走 Parquet 缓存层。

### 4.2 策略回测场景红线（仿真）

- 回测与实盘强对齐：交易规则、参数、风控约束、计算逻辑与实盘完全一致，禁止回测宽松、实盘严格。
- 全真市场模拟：强制开启滑点、手续费、涨跌停拦截、停牌过滤、资金容量限制，杜绝虚假回测。
- 时序绝对单向：严格按时间递增遍历，禁止回溯修正历史信号、禁止事后优化参数。
- 禁止曲线拟合：不允许针对单段历史行情优化参数，策略需跨周期、跨标的通用。
- 幸存者偏差过滤：回测标的池必须为当期实际可交易标的，禁止用退市后集合回填。
- 回测结果可复现：相同数据、相同参数、相同环境，多次回测结果完全一致。
- 样本外验证：必须预留一段独立的、未参与任何参数优化的"冷启动"数据进行最终验证，作为报告的一部分。

### 4.3 实盘交易场景红线（生产最高优先级）

- 风控优先于交易：所有下单、调仓逻辑必须前置多层风控校验，风控不通过直接拦截，不执行交易。
- 异常容错兜底：网络波动、接口超时、行情中断、订单异常必须有重试、兜底、告警机制，不爆仓、不重仓。
- 禁止逆势风险操作：禁止无止损持仓、禁止逆势加仓、禁止单次超限额重仓。
- 全链路日志留存：行情接收、因子计算、信号触发、风控校验、订单上报、成交结果全部留痕。
- 实盘参数独立隔离：实盘禁止直接使用研发、回测参数，需单独配置生产级安全阈值。
- 人工干预通道：必须提供紧急暂停、一键平仓的人工干预接口，且该接口权限高于所有自动化逻辑。
- 灰度发布：新策略或重大修改，必须先在小资金账户或模拟盘中运行观察，稳定后再逐步放大资金。

## 五、分场景开发规范

### 5.1 因子挖掘与研发规范

- 挖掘流程：假设驱动 → 数据准备 → 因子构建 → 单因子检验 → 中性化/正交化 → 去冗余 → 入库，逐步留痕。
- 数据准备：统一复权、过滤停牌/涨跌停/无量行情、多源交叉验证，避免因子失真。
- 标签构建：前向收益与持有周期严格对齐，禁止标签与特征时间窗重叠。
- 因子计算统一输出标准化结构化结果：时间、标的、因子值、有效性标记。
- 必须配套因子有效性检验：IC均值、IC胜率、IR比率、因子单调性、分位数收益、换手率、IC衰减稳定性校验。
- 支持多周期复用：1min/5min/1h/日线因子逻辑统一适配，无需重复编写代码。
- 中性化处理：截面因子必须考虑市值/行业/风格中性化，确保增量信息而非已知风险暴露。
- 因子相关性分析：开发复合因子前，必须分析各基础因子间的相关性，避免共线性问题。

### 5.2 策略开发与回测规范

- 策略链路：因子信号 → 信号合成/加权 → 组合构建 → 成本与容量评估 → 回测验证，逻辑分层解耦。
- 回测执行事件驱动机制，模拟真实交易时序，杜绝未来信息套利。
- 标准化输出评价指标：累计收益、年化收益、最大回撤、胜率、盈亏比、夏普比率、最大连续亏损、交易频次、换手率。
- 成本敏感性分析：评估手续费、滑点对策略收益的侵蚀，判断策略容量上限。
- 支持样本外验证：必须使用未参与参数优化的时间段数据验证策略有效性。
- 回测结束自动生成报告，包含收益曲线、回撤曲线、交易明细、因子有效性总结。
- 压力测试：对策略进行极端行情（如2020年3月、2015年股灾）的压力测试，评估其在危机中的表现。

### 5.3 实盘交易规范

- 行情、信号、下单时序严格对齐，杜绝时序错乱导致的错单、漏单、延迟交易。
- 多层风控体系：单笔限额、单标的仓位上限、总仓位上限、单日最大亏损止损、连续亏损暂停交易。
- 订单状态全生命周期管理：下单、未成交、成交、撤单、异常状态全覆盖监控。
- 实盘禁止动态随意改参，所有参数修改需留痕、可追溯，支持版本回退。
- 交易成本监控：实时追踪滑点、手续费等交易成本，若偏离预期过大，触发告警。

### 5.4 LangGraph 智能交易Agent规范（可选）

- 区分研发/回测/实盘三种运行模式，通过配置切换，禁止硬编码区分环境。
- 交易状态统一由State管理，禁止全局变量传参，状态增量更新、不整体覆盖。
- 节点单一职责拆分：数据加载、因子计算、信号生成、风控校验、订单执行、结果记录。
- 所有节点捕获异常，异常信息写入状态日志，实盘场景异常自动熔断，避免风险扩大。
- Agent行为审计：每个Agent决策步骤（思考链、中间结果）都应被记录，便于事后复盘和问题定位。

## 六、分场景测试规范

- 因子测试：覆盖正常行情、极端行情、空数据、停牌数据，验证因子无未来、数值稳定、结果可控。
- 因子回归测试：因子逻辑修改后必须重跑 IC/单调性基准，确保无逻辑漂移、无数据泄露。
- 回测测试：固定测试数据集，验证迭代后指标无异常突变、逻辑无漂移、无数据泄露。
- 实盘逻辑测试：模拟网络异常、行情缺失、风控触发、订单失败场景，验证兜底机制生效。
- 回归测试（分级执行，2026-08-13 修订）：日常任务仅跑**受影响的模块/集成测试**，验证新改动不破坏相关功能；**全量回归**只在两类时机执行——① 发布前（里程碑版本 bump/晋级里程碑）必跑；② 每月底例行巡检一次。日常 build bump（`bump_version.py --build`）不触发全量回归。禁止每次任务都跑全量，避免测试时间随系统规模线性膨胀。
- slow 分级（2026-08-13 起）：重量级真实演化/回测测试统一标记 `@pytest.mark.slow`（当前 26 个，集中于 test_evolution_loop.py），日常回归用 `pytest tests/ -m "not slow"` 跳过、全量验收必跑；DuckDB 嵌入式单进程写约束下，xdist 多 worker 并发写 `factor_catalog_futures.duckdb`（L3 因子资产库）会锁冲突，日常回归建议单进程执行，锁冲突类测试单进程定向复核（`state.db` 已切 SQLite WAL 多读单写不互斥，2026-08-13 E.3 S2；L2/L3 DuckDB 写锁已由 E.4 S1 短连接 + filelock 跨进程串行化解锁，写冲突概率大降，锁冲突类测试仍建议单进程定向复核）。
- 性能基准测试：对关键计算路径（如因子计算、信号生成）设定性能基准（如每秒处理多少条K线），防止性能退化。
- 所有新增功能必须配套单元测试；日常验证命令：模块测试 `pytest tests/<模块>/ -v`、集成测试 `pytest tests/<集成路径>/ -v`；全量命令 `pytest tests/ -v`（仅发布前/月度巡检）；日常回归命令 `pytest tests/ -m "not slow" -q -o addopts="" -p no:cacheprovider`

## 七、项目运维命令规范

```text
# 代码格式化
ruff format src/ tests/

# 代码静态校验
ruff check src/ tests/

# 严格类型检查
mypy src/

# 全量单元测试（仅发布前/月度巡检执行）
pytest tests/ -v

# 日常验证：模块/集成测试（按受影响范围选择）
pytest tests/research/ -v    # 因子挖掘模块测试
pytest tests/backtest/ -v   # 回测模块测试
pytest tests/live_trade/ -v # 实盘模块测试
pytest tests/factor_engine/ -v   # L2 因子演化集成测试

# 性能基准测试
pytest tests/benchmarks/ -v --benchmark-only
```

## 八、受保护文件/目录（禁止擅自修改）

AI仅可读，如需修改需专项确认，禁止私自重构、删除、改动核心逻辑。

- `config/`：全场景配置模板、环境隔离规则、安全阈值配置
- `common/data/`：底层数据加载、清洗、时序对齐核心逻辑
- `live_trade/risk_control/`：实盘核心风控兜底规则
- `backtest/simulation/`：全真交易仿真底层逻辑
- 已上线稳定因子、实盘运行策略核心算法
- `docs/architecture_decisions.md`：架构决策记录（ADR），记录了关键设计选择的背景和理由

## 九、AI标准化开发工作流（四场景通用）

1. **需求理解与澄清**：与项目负责人沟通，明确本次开发的具体目标、验收标准和潜在风险点。
2. **确认当前开发场景**：因子挖掘 / 策略开发回测 / 实盘迭代 / 风控治理，明确场景专属约束。
3. **数据可用性确认**：确认数据源、周期、时间窗、复权方式，排除数据缺口导致的偏差。
4. **前置合规校验**：排查未来函数、数据泄露、数据窥探、时序错误、实盘风控漏洞。
5. **设计评审**：对于复杂模块，先编写设计文档或伪代码，进行团队内审，再进入编码阶段。
6. **按目录分层开发**，保证场景解耦、逻辑独立、参数可配置。
7. **补全类型注解**、业务注释、场景适配说明。
8. **编写对应场景的单元测试**，覆盖常规与边界异常场景。
9. **执行格式化、静态检查、模块/集成测试**（全量仅发布前/月度巡检），确保无报错、无规则违规。
10. **样本外/回测验证**：量化改动必须附带样本外验证或回测对比，作为验收依据。
11. **输出修改说明**、场景适配要点、测试结果、上线/使用注意事项。
12. **代码审查与合并**：发起Pull Request，邀请同事进行代码审查，重点检查逻辑正确性、统计严谨性、规范遵守情况和潜在风险。审查通过后合并。

## 十、Claude Code 兼容规则

本模板为量化全场景通用开放规则，Claude Code 只需在项目根目录 CLAUDE.md 中添加 @AGENTS.md，即可自动继承所有挖掘、投研、回测、实盘规范，实现多AI工具规则统一、无差异适配。
