# FTS 执行模式流程图

> 版本: v2.105.0+28
> 最后更新: 2026-08-05

## 三种执行模式

```
┌─────────────────────────────────────────────────────────────────┐
│                        CLI 命令行模式                            │
│                                                                 │
│  python -m fts.cli <command> [options]                          │
│                                                                 │
│  ┌──────────┐  ┌───────────┐  ┌──────────────┐  ┌────────────┐ │
│  │ version  │  │ monitor   │  │ evolution    │  │ portfolio  │ │
│  │ 查看版本  │  │ 健康检查   │  │ run          │  │ run        │ │
│  └──────────┘  └───────────┘  │ --max-gen N   │  │ 组合构建    │ │
│                                │ --universe U  │  └────────────┘ │
│                                │ --max-stocks S│                │
│                                └───────┬───────┘                │
│                                        │                        │
│                                        ▼                        │
│                                ┌──────────────────┐             │
│                                │ trace_id 自动生成  │             │
│                                │ 贯穿全链路         │             │
│                                └──────────────────┘             │
│                                                                 │
│  其他命令:                                                      │
│  ├── fts meta-loop run        # L1 手动触发                    │
│  ├── fts factor list/seeds    # 因子/种子查询                   │
│  ├── fts factor show <id>     # 因子详情                       │
│  └── fts scheduler run/list   # 调度管理                       │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                      Scheduler 定时调度模式                       │
│                                                                 │
│  fts/scheduler/engine.py  (APScheduler 驱动)                    │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │              调度计划（TRAE Schedule 执行）               │   │
│  │  ┌─────────────────────────────────────────────────────┐ │   │
│  │  │ L1 Meta-Loop     │ 每日 00:00 │ 知识补给+种子注入      │ │   │
│  │  ├─────────────────────────────────────────────────────┤ │   │
│  │  │ L2 种子+演化     │ 每日 01:00 │ 种子晋升+演化(10/50代) │ │   │
│  │  ├─────────────────────────────────────────────────────┤ │   │
│  │  │ L2 批量挖掘      │ 周日 06:00 │ BatchMiner 批量漏斗   │ │   │
│  │  ├─────────────────────────────────────────────────────┤ │   │
│  │  │ L2 批量子链评估  │ 周日 09:00 │ 全量 active 画像刷新  │ │   │
│  │  ├─────────────────────────────────────────────────────┤ │   │
│  │  │ L2 周度评审      │ 周日 10:00 │ 全量重审+衰减+收口     │ │   │
│  │  ├─────────────────────────────────────────────────────┤ │   │
│  │  │ 评审质检阀门+监控│ 每日 04:00 │ 机审+巡检+逻辑+数据    │ │   │
│  │  ├─────────────────────────────────────────────────────┤ │   │
│  │  │ L3 Portfolio     │ 工作日 05:00 │ 仅 approved 组合    │ │   │
│  │  ├─────────────────────────────────────────────────────┤ │   │
│  │  │ 期货数据同步     │ 工作日 17:30 │ 行情多源缓存更新     │ │   │
│  │  ├─────────────────────────────────────────────────────┤ │   │
│  │  │ 信号管道         │ 工作日 20:00 │ 横截面信号报告       │ │   │
│  │  ├─────────────────────────────────────────────────────┤ │   │
│  │  │ Health Check     │ 每 10 分钟 │ 状态监控             │ │   │
│  │  └─────────────────────────────────────────────────────┘ │   │
│  │                                                           │   │
│  │  ⚠ 内部调度默认停用（v2.104.0+99）：16 任务 enabled=False │   │
│  │    周期任务由 TRAE Schedule 定时自动化执行（时间与上表一致）│   │
│  │    （v2.105.0+3 合并后 TRAE Schedule 当前 8 Active + 6     │   │
│  │     Paused 由合并任务接管）                                │   │
│  │    一键启用：$env:FTS_INTERNAL_SCHEDULER_ENABLED="1";     │   │
│  │              fts scheduler run                            │   │
│  │    → 每个任务执行时生成独立 trace_id (前缀+时间戳)         │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌──────────────┐   ┌──────────────┐   ┌──────────────────┐    │
│  │ watch dog    │ → │ hotswap      │ → │ 任务执行+重试     │    │
│  │ 进程监控      │   │ 热重载配置    │   │ 3次重试+熔断      │    │
│  └──────────────┘   └──────────────┘   └──────────────────┘    │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                      Monitor 监控模式                            │
│                                                                 │
│  fts/monitor/  (HTTP 端点 + 定期巡检)                           │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  健康检查端点 GET /health                                 │   │
│  │  ┌─────────────────────────────────────────────────────┐ │   │
│  │  │ {                                                    │ │   │
│  │  │   "healthy": true,                                   │ │   │
│  │  │   "circuit_broken": false,                            │ │   │
│  │  │   "stale": false,                                     │ │   │
│  │  │   "loops": {                                          │ │   │
│  │  │     "L1": {"status": "running", "age_hours": 0.5},   │ │   │
│  │  │     "L2": {"status": "completed", "age_hours": 8},   │ │   │
│  │  │     "L3": {"status": "completed", "age_hours": 12},  │ │   │
│  │  │     "Signal": {"status": "completed", "age_hours": 4}│ │   │
│  │  │   }                                                    │ │   │
│  │  │ }                                                    │ │   │
│  │  └─────────────────────────────────────────────────────┘ │   │
│  │                                                           │   │
│  │  CLI 查看: python -m fts.cli monitor [--json]             │   │
│  │  HTTP 查看: http://localhost:9100/health                   │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Elite 因子追踪 (elite_tracker.py)                        │   │
│  │  ├── 自动淘汰: 连续 30 日 IC < 0 的 elite 因子            │   │
│  │  ├── 超载清除: elite 因子数 > 20 时自动清除低分因子       │   │
│  │  ├── 衰减检验: 监控因子预测能力衰减趋势                     │   │
│  │  └── 行为漂移: 检测因子逻辑漂移（逻辑监控）                 │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  熔断保护 (circuit_breaker.py)                            │   │
│  │  ├── Token 超 2x: 消耗超过预算 2 倍                        │   │
│  │  ├── 连续低 IC: 5 代 IC < 0.005                           │   │
│  │  ├── 失败率过高: 失败率 > 95%                              │   │
│  │  └── 连续低质量: 3 代质量分 < 30                           │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

## 模式选择

| 模式 | 适用场景 | 启动方式 |
|:-----|:---------|:---------|
| CLI 命令行 | 开发调试、手动执行、单次运行 | `fts evolution run --max-generations 3` |
| Scheduler 定时 | 生产环境自动运行（v2.104.0+99 起默认停用，周期任务由 TRAE Schedule 执行） | `$env:FTS_INTERNAL_SCHEDULER_ENABLED="1"; fts scheduler run` · 停用：`Remove-Item Env:FTS_INTERNAL_SCHEDULER_ENABLED` · 状态：`fts scheduler status` |
| Monitor 监控 | 持续监控、告警 | 内嵌于其他模式，自动启动 HTTP 端点 |

## 状态流转

```
[stopped] → [running] → [completed] → [circuit_broken] → [recovered/stopped]
     │                                              │
     └────────────────────── [paused] ──────────────┘
```

各循环状态持久化到 `memory/` 目录，通过 `fts monitor` 查看实时状态。

## 熔断策略

| 熔断类型 | 触发条件 | 恢复方式 |
|:---------|:---------|:---------|
| Token 熔断 | 消耗 > budget * 2.0 | 等待下一个调度周期 |
| 低 IC 熔断 | 连续 5 代 IC < 0.005 | 手动重启或等待次日 |
| 失败率熔断 | 失败率 > 95% | 检查配置后重启 |
| 连续低质量 | 连续 3 代质量分 < 30 | 手动重启 |

## 质量评分卡

因子晋升 elite 前需通过质量评分卡评估（50 分制）：

| 维度 | 分值 | 说明 |
|:-----|:-----|:-----|
| IC 稳定性 | 0-8 | IC 一致性 + IC 波动率 |
| Sharpe | 0-8 | 年化夏普比率 |
| 换手率 | 0-8 | 月度换手率 |
| 容量 | 0-5 | 资金容量评估 |
| 频率 | 0-5 | 数据频率 |
| 覆盖率 | 0-5 | 品种覆盖率 |
| 鲁棒性 | 0-6 | 跨品种/跨期稳定性 |
| 经济逻辑 | 0-5 | 四维经济逻辑评分 |

**分级标准**:
- A 级: ≥ 40 分 → 精英池（附加质量分）
- B 级: ≥ 30 分 → 精英池
- C 级: < 30 分 → 质检淘汰

## 一致性元数据

| 字段 | 值 |
|:-----|:----|
| 代码→文档映射 | 本文件描述 FTS 三种执行模式（CLI 命令行、Scheduler 定时调度、Monitor 监控），对应 `fts/cli.py`、`fts/scheduler/engine.py`、`fts/monitor/` 模块 |
| 可验证断言 | CLI 支持 `fts evolution run` / `fts portfolio run` / `fts meta-loop run` 等子命令；Scheduler 注册 16 个任务（L1 00:00 / 种子评估 02:00 / 演化 03:00 工作日+周六 / 批量 周日06:00 / 周度评审 周日10:00 / L3 工作日06:00 / 信号管道 工作日20:00 / 因子巡检 04:00 / 逻辑 04:30 / 数据级 05:00 / 数据质量 每5m / 健康检查 每10m / 期货数据同步 工作日17:30 / 流动性池 周六08:00 / MHF 每30m），股票 L3 19:30 与股票信号管道 08:45 已随股票管线剥离至 fts-stock（2026-08），时间与 TRAE Schedule 对齐；v2.104.0+99 起内部调度默认停用（`FTS_INTERNAL_SCHEDULER_ENABLED` 默认 "0"），TRAE Schedule 为唯一调度源；L1 任务含批量采集(≥300 篇/天)→粗筛→LLM 深读三层管线与失败复活（plans/44）；Monitor 提供 HTTP 健康检查端点 GET /health、Elite 因子追踪、熔断保护 |
| 检验方式 | 运行 `python -m fts.cli --help` 验证 CLI 命令；`python -c "from fts.scheduler.tasks import list_tasks; assert len(list_tasks()) == 16"` 验证调度任务数；`fts scheduler status` 查看实际调度数 |