# FTS 差距分析

> 版本: v3.0.0+25

> 校正说明（2026-08-20 v3.0.0+17）：全量瘦身校正——① 删除全部早期（v2.104.0 前关闭）差距条目（主表 GAP-001~113、GAP-X01~X03、GAP-I 系列 20 项、GAP-L 系列 10 项）与第 3 节差距详情（GAP-001~045 已全部关闭），历史关闭记录保留于 07-operations.md 版本历史与 git 历史；② 编号冲突重编号——P1 表 GAP-152（宏观 Beta 层）/P0 表 GAP-153（拥挤度体系）与 P2 表 GAP-152/153（hold 数据字段批次）冲突，重编号为 GAP-176/177；P1 表 GAP-154~158（2026-08-20 环境兼容问题）与 P2 表 GAP-154~158（QuantData/IM0/high_vol 等）冲突，重编号为 GAP-169~173；原 L 系列 GAP-161/162（scope_domain）与 P1 表 GAP-161/162（plans/59）冲突，重编号为 GAP-174/175；③ 删除过时总览日志（保留最新一条）；④ 总览口径统一：开放 = 状态列非 ✅（🔴 开放 / 🟡 部分 / 🔄 处理中均计开放）。

> 总览更新（2026-08-20 v3.0.0+17，GAP-161~168 登记，plans/59 评审质检体系优化）：**因子评审质检体系审查（factor_lifecycle.md §3-§7 全链）发现 6 处薄弱点映射 8 条优化项（plans/59）——① 阈值静态化未按 regime 分层（GAP-161）；② 跨运行累积家族错误率无控制（GAP-162）；③ 特异因子无晋升后 OOS 前瞻二次确认（GAP-163）；④ IC 类判定多处重复无差异告警（GAP-164）；⑤ 数据质量监控与评审割裂（GAP-165）；⑥ 人审无超时 SLA（GAP-166）；⑦ 参数稳健性仅离散档位（GAP-167）；⑧ 成本/容量静态失真（GAP-168）。8 项均登记开放（P1 6 项 GAP-161~166 + P2 2 项 GAP-167/168），P1 开放 3→9、P2 开放 5→7、合计开放 9→17（已关闭维持 180、总计 189→197），详见 P1 登记表 / P2 登记表。**

> 最后更新: 2026-08-20
> 状态: 活跃 — 随项目迭代持续更新

---

## 1. 差距总览

| 优先级 | 开放 | 已关闭 | 总计 |
|:-------|:-----|:-------|:-----|
| P0 | 0 | 2 | 2 |
| P1 | 11 | 29 | 40 |
| P2 | 14 | 15 | 29 |
| GAP-C（Stage 3C 远期） | 1 | 7 | 8 |
| **合计** | **26** | **53** | **79** |

> 总览口径（2026-08-20 校正）：开放 = 状态列非「✅」项（🔴 开放 / 🟡 部分 / 🔄 处理中）；已关闭 = 状态列为「✅/已实施/已修复」且标注关闭版本 ≥ v2.104.0（更早关闭项已按全量瘦身删除，历史记录见 07-operations.md 版本历史与 git 历史）。P0 开放 0（GAP-177 以纯观测层形式实施完成）；P1 开放 11（GAP-132/150/151/161~166/170/173）；P2 开放 14（GAP-089/090/093/127/142/152/153/156/157/158/167/168/174/175）；GAP-C 开放 1（C4 真实多机集群后置）。

---

## 2. 差距登记表

### P0 — 阻塞性问题（影响核心功能）

| ID | 模块 | 差距描述 | 影响 | 处理期限 | 状态 |
|:---|:-----|:---------|:-----|:---------|:-----|
| GAP-177 | `fts/factor_engine/regime_crowding.py`（新增）+ `portfolio_loop.py`（Step 2.5 + build_combo 乘性链）+ `futures_signal_pipeline.py`（Step 3h1.6）+ `config/settings.yaml`（l3.regime_crowding） | 拥挤度体系缺位（plans/54 P1-2 立项，外部 Regime-Driven §7.2）：① 6 信号不全（volume_stall/vol_structure 缺失、corr 静态强度、oi 无天量、turnover 仅 seed 不可控）；② 无拥挤×置信度联合门控；③ crowding_score 全 abs() 取模多空方向被抹掉（无法识别逼空）；④ position_rank_crowding 独立模块未接入主流程；⑤ 无拥挤期止损收窄 | 组合在趋势透支时无防御：高拥挤期仍满仓顺向敞口，拐点集中回撤 | 1 月内 | ✅ 已关闭（v2.105.0+25，plans/56 A-D 四模块实施完成：**A** `regime_crowding.py` 6 信号合成器（corr 逐品种对滚动相关趋近/放量滞涨 ratio 口径/动量差走弱/波动突升/OI 天量/换手分位）+ 方向分解（多头/空头拥挤逼空识别）+ 合成 score；**B** 联合门控 build_joint_gate_scale（高置信+高拥挤 0.5 / 低置信+高拥挤 0.0）并入 build_combo 乘性链（exposure×aligned×beta×crowding）；**C** 信号管线 Step 3h1.6 多空方向偏置（减多不抢反弹/减空不追空）+ `--enable-regime-crowding`；**D** settings 配置（enabled=false 默认关）+ Prometheus crowding 指标 + `scripts/regime_crowding_predictive_power_check.py`（K-W 决策门 + 事件研究）；新增测试 17 用例（6 信号 16 + build_combo 乘性 1）+ 受影响回归 144 全绿 + ruff 通过。**决策门两轮未通过（原参数高拥挤样本不足 + 校准后 K-W p=0.79 不显著、事件研究命中率 50%/误报率 68.6%）——量价拥挤度捕捉"高波动/高参与"状态而非崩盘前兆；正式降级为纯观测层**（enabled=false 不干预组合，仅 Prometheus crowding_scale/crowding_state + 报告观测；GAP-069 会员持仓拥挤度保留独立价值）；校准参数保留（high_crowding=0.4/分位 0.75 使 score 有分布）） |
| GAP-081 | `fts/factor_engine/expr_dsl/registry.py` `A_SHARE_FIELDS` + `seeds/stock/{northbound,margin_trade,holder_count,analyst_revision}.yaml` | A 股特有字段零数据源：`A_SHARE_FIELDS` 10 字段（northbound_flow/northbound_hold_pct/margin_balance/margin_net_buy/margin_short_balance/holder_count/analyst_up_count/analyst_down_count/analyst_total_count/analyst_eps_revision）仅注册 L0 恒等算子，fts/data_sources 与 FundamentalProvider 零接入（grep 命中 0）；69 个种子因子（4 族 YAML）已上线但面板缺列 `_ArrayDataWrapper.__getitem__` 抛 KeyError → 信号被跳过 | 北向/两融/股东户数/分析师预期四类 A 股特有因子全部空转，股票因子体系缺资金流与预期维度（与头部机构差距 P0） | 1 月内 | ✅ 已关闭（v2.104.0+1 迁移关闭：股票剥离后 A 股特有字段接入已于 fts-stock v2.104.0 实施——`fts/data_sources/ashare_special_source.py` 两融族（AKShare stock_margin_detail_sse/szse + 派生 margin_net_buy/margin_short_balance）/股东户数族（holder_count，以公告日期为可用日防未来函数）/北向族（northbound_hold_pct/northbound_flow）/分析师族（analyst_total_count/analyst_eps_consensus）+ `migrate.py` GAP-081 缓存建表注释；主系统 `A_SHARE_FIELDS` 保留为 DSL 字段注册供 fts_core 复用，无 A 股数据路径） |

### P1 — 重要改进（提升效率或稳定性）

| ID | 模块 | 差距描述 | 影响 | 处理期限 | 状态 |
|:---|:-----|:---------|:-----|:---------|:-----|
| GAP-176 | `fts/factor_engine/macro_regime.py`（已实现未接线）+ `config/regime_routing_rules.yaml`（global_rules 零消费死配置）+ `fts/factor_engine/regime_beta_layer.py`（新增） | 宏观 Beta 层缺位（plans/54 D1 立项，外部 Regime-Driven §1.3 Beta 优先）：FTS 是纯 Alpha 因子加权系统 + Regime 调制器（权重倍率/总仓位缩放/子链方向 Gate），**未"顺 β 方向配置敞口"**——① 无 RISK_ON/RISK_OFF/RANGE_BOUND 宏观状态层（`global_rules` 三态风控零代码消费）；② bear 制度只降权/降杠杆/降仓位，不转空/不配置负 Beta 敞口（期货多空双向下负 β 应反向做空进攻）；③ `macro_regime.py`（Bridgewater PMI/CPI 四象限）已实现但无运行时消费方；④ 无股债比 risk_pref（IF/T）风险偏好信号；⑤ 组合报告/Prometheus 无宏观层状态与敞口缩放留痕 | 组合在宏观风险偏好层面无"顺势/逆势"概念：RISK_OFF 期仅被动降仓不反向进攻、RISK_ON 期无顺风放大；宏观状态不可观测、死配置空转 | 1 月内 | ✅ 已关闭（v2.105.0+22，plans/55 A-E 五模块：**A** `regime_beta_layer.py` BetaDetector——金融期货合成指数（IF0/IH0/IC0/IM0/TF0/TS0 等权归一化）趋势 MA20-MA60 + 20 日 realized vol 历史分位（门控）+ IF0/TF0 股债比滚动 z-score（risk_pref 调整为 FTS 池内品种）→ 软投票三态 + min_confidence 门槛；**B** 信号管线 Step 3h1.5 `apply_beta_bias` 多空不对称（RISK_OFF 多头 ×0.6/空头 ×1.2 反向进攻，`--enable-regime-beta` 灰度）；**C** 组合层 `build_combo` beta_scale 乘性并入（exposure_final = exposure_scale × aligned × beta_scale，RISK_OFF off_scale=0.5 压缩总敞口，regime_meta 落盘 beta_state/beta_scale）；**D** 实盘风控 `BETA_RISK_PARAMS` 档位叠加（RISK_OFF 杠杆 ×0.7/单日亏损 ×0.7，`RiskManager(config, regime, beta_state)` 注入）；**E** settings `l3.regime_beta_layer` 配置（默认关）+ Prometheus fts_beta_state/fts_beta_scale + `scripts/regime_beta_predictive_power_check.py`（K-W 决策门）；新增测试 22 用例（Beta 层 13 + 风控档位 8 + build_combo 乘性 1）+ 受影响回归全绿 + ruff 通过；二期：macro_regime 四象限慢层/实盘 http_server 传 beta_state） |
| GAP-147 | `fts/factor_engine/factor_inspector.py`（`FactorReviewWorkflow._conn`）+ `fts/scheduler/jobs.py`（`_review_gate_weekly`）+ `fts/monitor/http_server.py`（人审工作台 C8） | 评审质检阀门错库：`FactorReviewWorkflow._conn()` 无 db_path 时 `schema.get_connection(None)` 落到默认库 `factor_catalog.duckdb`（futures 186/stock 135/multi 1，无 energy 因子），与 `FactorRepository(market=...)` 路由的市场库（`factor_catalog_{energy,futures}.duckdb`）不一致 → `review_l3_pool`/`list_pending`/`review_inplace`/`_decide`/`_delete_review` 在错误库查询/读写全部空转 | L2→L3 评审质检阀门（pending 机审 + approved 复核）对 energy/futures 静默失效：2026-08-18 每日 04:00 合并任务实测 `_review_gate_weekly('energy')` scanned=0/auto_reviewed=0 空跑（三库均无评审痕迹）；http_server 人审工作台 pending/history 同样读错库 | 1 周内 | ✅ 已关闭（v2.105.0+12：`_conn()` 改 `schema.get_connection(self._db_path or self._repo._db_path)`——显式 db_path 优先（CLI/测试/演化链路不受影响），否则与因子仓库同库按 market 路由，修复 review 表与因子库跨库漂移；补跑当日 energy 机审 `scanned=328`/`demoted=69`（metadata.qa_review 质检记录缺失宁缺毋滥撤销 approved 转人审退回 L2 冷却池）/`auto_reviewed=70`，approved 池 328→259、pending 1→70，futures/默认库零污染；评审链路 80 用例 + scheduler test_jobs 42 用例全绿 + ruff 通过） |
| GAP-144 | `fts/factor_engine/evolution_candidate.py`（`_process_candidate` Step 4/4.5）+ `fts/factor_engine/evolution_seeds.py`（`_evaluate_cross_section` + 种子路径 Verifier/评分卡）+ `config/settings.py`（`l2_subchain_waiver_enabled`） | L2 晋升入口无子链放行：晋升判定（Verifier `min_ic=0.03` / 质量评分卡全链 IC 打分）在**晋升前**用全链 IC，而子链画像（`subchain_ic_report`，t 检验三门槛）在评估链已产出但 Verifier/评分卡不消费——单链特异/部分链因子全链 IC 被无效/反向子链稀释 < 0.03 时被 Verifier 直接拦截，**根本走不到晋升落库落画像那一步**；plans/49 §B2 的单链特异放行仅在机审/评审环节（`AutoReviewPolicy.classify`），不覆盖晋升入口 | 子链特异因子（某链 IC 强但其它链反向/近零）被系统性错杀，无晋升机会；与 plans/47 §A 实证「10 个单链特异、52 个部分链有效」的真实收益来源相悖 | 1 周内 | ✅ 已关闭（v2.105.0+8：① 演化路径 `_process_candidate` Step 4 Verifier 判定后接入 `_apply_subchain_ic_waiver`——仅 energy + `l2_subchain_waiver_enabled`（env `FTS_L2_SUBCHAIN_WAIVER_ENABLED`，默认关）+ 存在 effective 子链时豁免 IC/ICIR 稀释维度（Sharpe/回撤/OOS/单调性等其它维度失败仍拦截），Step 4.5 评分卡用 `_subchain_waiver_view`（effective 子链 max \|mean_ic\| 替换全链 ic）防 C 级淘汰；② 种子路径 `_evaluate_cross_section` IC 门槛经 `_subchain_waiver_pass` 豁免 + 种子 Verifier 判定接入同构 `_apply_seed_subchain_waiver` + 评分卡同放行视图；③ `subchain_waiver` 标记写入 evaluation 留痕；新增 test_subchain_waiver.py 16 用例 + 受影响回归 290 全绿 + ruff 通过） |
| GAP-145 | `fts/factor_engine/regime_validation.py`（G7 已实现零调用）+ `regime_profile.py` + `regime_conditional_weight.py` + `evaluation_chain.py` + `evolution_promote.py` + `portfolio_loop.py`（plans/53） | Regime 条件化因子交易缺位：① G7 `validate_factor_across_regimes`（分制度 IC/ICIR）与 `validate_regime_predictive_power`（制度预测力 K-W 检验）已实现但**零调用方**；② 因子无"×制度"画像落库（对比 symbol_ic/subchain_ic_profile 已落库）；③ 组合层 regime 仅做权重倍率微调（0.7~1.3×），**不剔除当前制度下 IC 为负/弱的因子**（仅 energy 子链 gate 整链归零）；④ 晋升无 regime 门槛，单制度过拟合因子可晋升 elite；⑤ 胜率非显式评估维度 | 当前市场制度下逆制度因子（IC 显著为负）仍全权重参与组合，拖累胜率与回报——用户目标"Regime 条件下胜率/回报更高的交易"无法落地 | 1 周内 | ✅ 已关闭（v2.105.0+9，plans/53 D/A/B/C 四模块：**D**——`RegimeSeriesBuilder`（规则检测 lookback=60 滚动构建制度序列）+ `scripts/regime_predictive_power_check.py` 决策门，energy 面板实测 **K-W p=0.030 通过**（high_vol +1.38% / oscillate -0.29% / low_vol -0.07% 条件前向收益）；**A**——`regime_profile.py` 画像计算器（IC/ICIR/**胜率**/scope，复用 G7 `_regime_icir` 单一实现，min_abs_ic 实施校准 0.03→0.05 滤噪声）+ 评估链 `regime_ic_report` 报告段（开关 `l3_regime_ic_report_enabled` 默认关 + energy 面板限定防批量评估性能退化）+ 晋升 metadata 落库；**B**——`regime_conditional_weight.py`（zero/soft 降权，scope=all/unknown/无画像不误杀）+ Step 2.5 后乘性应用（`l3.regime_conditional.enabled` 默认关）；**C**——`regime_gate_passed` 晋升硬门槛（有效制度数 ≥ `regime_profile.min_positive_regimes`=2，scope 非 list 放行）；新增 test_regime_profile.py 19 + test_regime_conditional_weight.py 10 + test_portfolio_loop +2，受影响回归 380 全绿 + ruff 通过） |
| GAP-143 | `fts/factor_engine/portfolio_loop.py`（`inject_to_fdt`）+ `scripts/futures_signal_pipeline.py`（`_compute_composite_scores`） | L3 子链差异化权重调制静默失效（键不一致 + 未接线）：① **键不一致**——`build_subchain_weights` 以 `factor_id`（fct_xxx）为矩阵键，`inject_to_fdt` 原样透传写入 `factor_weights.json`，而信号管线 `_compute_composite_scores` 以 `name`（fut_xxx）消费 → `subchain_weights.get(name)` 恒 miss 回退全权重 1.0，plans/47 §B 调制矩阵在生产链路实际不生效（2026-08-17 实测 energy/factor_weights.json 的 `weights` 键=fut_xxx 而 `subchain_weights` 键=fct_xxx）；② **主链路未接线**——`apply_subchain_modulation`（3D 信号矩阵左乘）定义但主链路 `build_combo` 前从未调用，Step 2b 仅标注 signals 不调制组合权重 | 特异因子在无效子链的负贡献不降权/归零，plans/47 子链差异化设计在生产链路失效；组合持续暴露无效子链 | 1 周内 | ✅ 已关闭（v2.105.0+5：① `inject_to_fdt` 经 combo.signals 的 factor_id→name 映射归一 subchain_weights 键（无 factor_id 时保留原键兼容旧产物），与 factor_weights.json weights 键及管线 name 消费对齐；② 端到端键链路测试验证 build→inject→load→composite 调制真实生效；test_subchain_weight.py 21→24 用例 + 受影响回归 317 全绿） |
| GAP-140 | `tests/` 全量回归 93 个预存失败（`test_auto_review.py` + `scheduler/test_engine.py` + `scheduler/test_jobs.py` + `scheduler/test_jobs_mhf.py` + `data_sources/test_tick_cache_accumulate.py` + `data_sources/test_tqsdk_tick_source.py` + `factor_engine/extractors/*` + `test_evolution_loop.py` + `test_portfolio_loop.py` + `test_factor_db.py` + `test_e2e.py` + `test_http_server.py` + `monitor/*` + `test_package_init.py` 等） | 全量回归 93 个**预存失败**（2026-08-17 实测 7748 passed / 93 failed），根因六类：① v2.104.0+89 评审门禁语义变更未同步 C8-2 旧测试（`AutoReviewPolicy.classify` 缺 qa_meta→转人审，test_auto_review 6 个仍断言 approved）；② v2.104.0+98 内部调度停用未同步 E2E（test_e2e test_10 断言 l2_evolution_loop）；③ v2.104.0+103 全局默认市场 energy 致市场路由断言过期（test_factor_db/promote/merge ~5 个）；④ GAP-133（+107）走航回退致 900 行断言过期；⑤ scheduler engine/jobs 40+ 与 portfolio_loop 3 个日志断言 caplog 空（全量顺序污染，单独跑全绿）；⑥ 环境依赖（TQ/tick 本地不可用 10 个、LLM mock 4 个、随机种子 1 个、并发 bump 竞态 1 个） | 全量回归门禁不可作发布验收（被预存失败淹没）；CI 全量步骤恒红掩盖真实回归信号 | 1 月内 | ✅ 已关闭（v2.105.0+7，专项完成，97 failed→0 failed 全量清零：① **评审门禁语义同步**——test_auto_review 6 个补 qa_meta 断言对齐 `AutoReviewPolicy.classify` 转人审语义；② **调度停用断言**——test_e2e test_10 改名 + monitor `task.enabled is False` 对齐 v2.104.0+98 内部调度停用；③ **市场路由**——全局默认 market futures→energy（v2.104.0+103）致 test_factor_db 默认库路径/promote/merge/mhf 断言全部显式传 market 或改 energy 断言；④ **走航回退**——GAP-133 数据不足回退致 900 行断言过期，test_build_wf_config 双重断言（1400 默认 3 年窗 / 750~1367 回退 1 年窗）；⑤ **caplog 污染根治（核心）**——根因 `import tqsdk`→`shinny_structlog` 模块级替换 `logging.getLogger`/`Logger.root`/`Logger.manager`/`loggerClass` 致 pytest caplog 失效，`tqsdk_source._import_tqsdk_safe()` 快照-恢复隔离模式替换 4 文件 7 处 import（tqsdk_source/tqsdk_tick_source/tqsdk_enhance_source/tqsdk_mhf_executor），scheduler engine/jobs 41 用例 caplog 断言恢复；⑥ **环境依赖**——tick_cache/tqsdk 测试固定日期 2026-08-07 超 7 天保留期被清理，改动态基准日期；extractors LLM narrative 语义同步；evolution_mode 断言 operator_first + hybrid 经配置实例 monkeypatch（get_config 缓存单例 setenv 不生效）；operator_evolution 随机种子自身通过；**附带 GAP-135 门禁影响 5 个晋升路径测试文件**（test_l2_elite_redundancy/test_l2_orthogonalize/test_risk_tag/test_structure_cluster_quota/test_evolution_l1_merge/test_evolution_loop TestShadowPool）加 `_disable_qa_gate` autouse fixture 或 mock 父因子/walk_forward 字段；**表达式去重种子豁免**——`evolution_promote` 对 source∈(seed,bootstrapping) 豁免去重（幂等初始化由 DuckDB 名称检查兜底）+ test_structure_cluster_quota 配额放行夹具用信号同款代码可区分变体避免误拦截；全量回归 `pytest tests/ -q` 0 failed 清零） |
| GAP-138 | `fts/factor_engine/regime_gate.py`（+ `gate_scale_map`）+ `fts/factor_engine/portfolio_loop.py`（Step 2.5）+ `factor_weights.json`（subchain_weights 输出） | L3 权重源头未消费子链方向 Gate：plans/48 的 Gate（`apply_subchain_gate`）仅在**信号管道** Step 3h1 硬生效（avoid 剔除/降权 + long/short 方向过滤），L3 产出的 `factor_weights.json` 中调制矩阵 **m[factor][子链]**（plans/47）只携带"质量×幅度"语义、未并入 Gate 回避决策 → avoid 子链在权重源头仍保留调制系数 1.0，方向回避完全依赖下游信号层补救（职责倒挂）；且 q（质量）/m（幅度）/g（方向）三层矩阵在 L3 中割裂（g 仅作 `subchain_gate_distribution` 观测段，不参与权重） | avoid 子链在权重源头不回避（factor_weights.json 调制系数恒 1.0），Gate 语义仅靠信号层补救；三层矩阵未闭合，扩展到其它产业链时新链 avoid 同样权重源头不感知 | 1 月内 | ✅ 已关闭（v2.104.0+113，plans/50 A/B 模块：A `regime_gate.py` 新增 `gate_scale_map` 纯函数——Gate 决策 → 链级缩放系数（avoid+hard→0.0 / avoid+soft→soft_avoid_ratio / long·short·neutral→1.0，方向过滤属信号层职责权重层不重复）；B `portfolio_loop.py` Step 2.5 接线 `_merge_gate_scale_into_modulation`——Gate 开启 + energy + 子链 regime 检测成功时将 m'[factor][子链] = m × gate_scale（avoid 链权重源头归零/降权），同步更新 signals 的 subchain_weights 标注与 factor_weights.json 输出；依赖 Step 2b 调制矩阵存在（enable_subchain_weight）否则保持观测语义零行为变更；质量报告新增 `subchain_gate_scale` 段（与 subchain_exposure/gate_distribution/quality_matrix 四网合一）；新增测试 11 用例 + 受影响回归 358 全绿，详见 plans/50） |
| GAP-137 | `fts/factor_engine/qa/pre_entry.py`（Q10）+ `qa/quarterly_check.py`（F6）+ `factor_inspector.py`（机审/退化）+ `factor_lifecycle.py`（IC 衰减）+ `energy_qa_review.py`（统一管道退化）+ `factor_catalog.metadata`（scope 静态化） | 评审质检与生命周期管理未子链化：plans/47 已建 m[factor][子链] 调制矩阵、plans/48 已建 g[子链] 方向 Gate，但评审质检（Q10/F6「板块方向一致」、机审全链 IC 门槛、准入分类）与生命周期（全链 IC 衰减/退化检测/退役红线）仍为**全链单值口径**——① 单链特异因子全链 IC 被稀释 < min_ic 被机审/评分卡/Q10 系统性误杀（47 实证 10 个单链特异因子）；② 全链衰减检测双向掩蔽（有效链失效被无效链稀释不触发=假阴性、单链失效触发整因子降级=假阳性）；③ `subchain_scope` 画像晋升时一次性落库后静态化，检测到单链失效不收缩 scope → 47 调制矩阵永远用旧画像，质量退化不传导权重；④ 评审/生命周期/调制（47）/Gate（48）互不联动无闭环 | 单链特异因子（真实收益来源）被系统性惩罚/误杀；失效子链暴露不剔除持续产生负贡献；scope 静态化致权重矩阵陈旧 | 1 月内 | ✅ 已关闭（v2.104.0+112，plans/49 A/B/C/D 四模块：A 存储底座（`subchain_factor_quality` 表 + `SubchainQualityRepository` + `build_subchain_quality_rows`，评估单元=(factor_id,market,chain)，晋升写首行/评审重算）；B 评审张量化（Q10 两级判定 `judge_q10_subchain` + 机审单链特异放行 + 准入三级权重 0.10 + F6 两级判定）；C 生命周期张量化（`compute_subchain_degradation` 单元粒度退化 + `_shrink_scope` 闭环 + 退役红线）；D 监控三网合一 + 子链定义参数化可扩展；灰度 `l3.subchain_quality.enabled=false` 默认关回退全链原逻辑；新增测试 46 用例 + 受影响回归 448 全绿，详见 plans/49） |
| GAP-135 | `fts/factor_engine/evolution_promote.py`（晋升准入）+ `fts/factor_engine/qa/`（QA 评审/审计）+ `fts/factor_engine/gp_evolver.py`/`evolution_loop.py`（GP 演化） | GP 因子评估链/晋升准入不一致 + 晋升期缺同表达式去重：① **QA 结论与入库矛盾**——2026-08-17 能化链演化晋升的 2 个 GP 因子（fct_6f207959/fct_55a1943c）`qa_review.q1_q10_passed=false`、结论「禁止入库」（Q2 逻辑文档化未过：economic_logic 缺失），但仍被晋升入库（promoted_at 落库 + elite JSON 快照写入），晋升路径未以 q1_q10 结论为准入门禁；② **审计因果项异常跳过**——两因子 audit causal_validity 均「因果检验执行异常: 因子代码为空」（代码实际存在），GP 因子审计代码读取疑似缺陷，因果维度恒 skipped；③ **晋升期缺去重**——同批次两个表达式完全相同（`rank(low)`，树深 1/规模 2，参数与回测指标逐位一致）的因子仍双双晋升，信号相关≈1.0 重复因子占用精英名额 | QA 门禁形同虚设（「禁止入库」结论不阻止晋升）；审计覆盖缺失（GP 因子因果项恒跳过）；重复因子污染精英池/L3 组合（相关 1.0 重复计权、稀释组合多样性） | 1 周内 | ✅ 已关闭（v2.105.0：**① 准入门禁**——`EliteStore._promote_to_elite` 以 q1_q10 结论为准入门禁：`build_qa_review` 透出 `one_vote_failed`/`scoring_failed`/`scoring_pass_ratio`，`l2_qa_gate_enabled` 开启时 Q1-Q10 一票否决项非空即拦截晋升（结论「禁止入库」不再被旁路，同日 3 个 A 级因子两处不一致根治）；**② 审计代码读取修复**——`audit.py` causal_validity 由 `expr` 读取改 `code` 编译补齐（GP 因子代码实际存在不再「因子代码为空」异常跳过，因果维度恢复真实审计）；**③ 晋升期表达式去重**——`find_duplicate_expression`（`normalize_expression` 规范化比对既有 elite JSON 快照）命中即拒绝晋升，防同批次/跨批次重复因子占用精英名额；种子导入（source∈seed/bootstrapping）幂等初始化豁免（DuckDB 名称检查兜底）；**生成端去重前置**（v2.105.0+2）——Step 1.35 `_is_generated_duplicate` 评估链前拦截（懒加载 elite 表达式集 + 本 run 已生成/已评估，命中记录 failure_trace/expr_duplicate/父因子 UCT 惩罚反馈），晋升端保留兜底；新增 test_gap135_qa_gate.py 13 用例全绿 + test_evolution_l1_merge/loop 同步；同日已手动退役 fct_55a1943c 的治标动作保留，根因修复后无需再人工干预） |
| GAP-136 | `fts/factor_engine/regime_gate.py`（新增）+ `scripts/futures_signal_pipeline.py`（Step 3h1）+ `fts/factor_engine/portfolio_loop.py`（Step 2.5 子链路由 + 质量报告） | 品种/子链 Regime 未作独立方向 Gate + 置信度未映射暴露：`SectorRegimeSelector.detect_all` 子链 Regime 仅"软票合成全局主制度 + 品种-链对齐度 ±0.20 blend 微调"——低置信度子链不回避（子链分歧被全局软票中和，方向偏置同时作用于 bull/bear 子链产生错误全局干预）、品种置信度不决定仓位（高置信满暴露/低置信收缩职能未发挥）、`REGIME_STYLE_MULTIPLIERS` 为全局 FactorStyle 乘数未下钻子链（该链该 regime 下激活哪些收益来源族未知，赚什么钱不可见） | 能化四子链驱动逻辑不同（能源 bull/煤化工 bear 并存时被中和）；低置信度子链品种不收缩暴露；收益来源族（赚什么钱）未按子链激活——Regime"能否×赚什么"仅完成一半（plans/47 只做幅度层） | 1 月内 | ✅ 已关闭（v2.104.0+111，plans/48 A/B/C/D 四模块：A 子链方向 Gate（`regime_gate.py` build_subchain_gates/apply_subchain_gate，long/short/avoid/neutral + 盲测池默认回避，信号管线 Step 3h1 接入仅 energy 生效、失败降级）；B 品种暴露缩放（`map_confidence_to_exposure` 分段 <0.4→0/0.4~0.7 线性/≥0.7→1.0 × `apply_exposure_scale` 品种-链对齐度，score=0 跳过与 avoid-soft 保留降权结果防双重惩罚）；C 收益来源族激活（`regime_adaptive_weight_adjustment(subchain_regimes=...)` 按因子 subchain_scope 路由子链 regime 倍率，数据不足回退全局；`build_subchain_return_source` 画像入 _regime_meta）；D 灰度（`--enable-regime-gating` 默认关）+ 与 plans/47 正交串联验证 + 质量报告 `subchain_gate_distribution` 段；新增测试 36 用例 + 受影响回归 694 全绿 + ruff 全绿，详见 plans/48） |
| GAP-134 | `fts/factor_engine/portfolio_loop.py`（L3 quality_weight 全链统一权重）+ `factor_catalog.metadata`（无 subchain_scope）+ `cross_section_evaluate_backtest`（期货/股票共用 IC 口径） | 子链特异因子被全链统一权重稀释/反向：2026-08-17 能化链 elite 实证 196 因子按 symbol_ic 四子链聚合，10 个单链特异（`fut_time_series_momentum` 油化工 +0.209/聚酯链 -0.070 反向、`fut_up_day_ratio` 油化工 +0.393/煤化工 0.000）、52 个部分链有效、仅 132 个全链同向；L3 仅 Step 1.8b 子链去冗余（限数量 ≤2），无子链差异化权重，特异因子在无效子链全额暴露产生负贡献；因子无适用性标记；期货（12–82 品种、每子链 3 品种）与股票（300 个股）截面样本差一个量级却共用 IC/审计阈值，子链均 IC 为小样本估计未做显著性校验即被采信 | 全链使用特异因子在无效子链产生负贡献、组合 IC 被稀释；跨市场 IC 口径失真（期货 0.3 与股票 0.05 不可比）；子链均 IC 噪声被当特异采信 | 1 月内 | ✅ 已关闭（v2.104.0+109，plans/47 四模块实施完成：A 子链画像+显著性护栏（`subchain_profile.py`+A2 落库）/ B L3 子链差异化权重（`subchain_weight.py`+PortfolioLoop+信号管线+CLI+配置，灰度默认关）/ C 期货股票评估口径分离（get_eval_config）/ D 暴露监控+特异因子受限使用；新增测试 50 用例 + 受影响回归 340+ passed，详见总览关闭段） |
| GAP-133 | `fts/scheduler/jobs.py` `l2_seed_promotion_energy_job`/`l2_batch_mining_energy_job`（`get_futures_panel(days=500)`）+ `fts/factor_engine/audit.py` `_check_oos_consistency` | 能化链 L2 种子评估 500 日短样本 1 窗口审计拦截（GAP-073 在 energy 链复现）：energy 作业 `days=500` 约 2 年共同窗口下 `_build_wf_config`（[2,3) 年分支 window_years=1/step=3mo）只能切 1 个走航窗口，v2.104.0+44 将 n_windows<2 改 failed 硬拦截后全部种子审计 oos_consistency 恒失败（2026-08-17 三次实测 30/30/184 因子 pass_rate=0.43、晋升 0；GAP-121 扩展 reaudit 300 天窗口 130 因子同因全失败） | 能源链 L1→L2 种子晋升通道系统性阻塞（0 晋升），演化无父因子补给；连带能源链 300 天 reaudit 全部因子被拦 | 1 周内 | ✅ 已关闭（v2.104.0+106：① `_L2_TRAINING_PANEL_DAYS=750`（energy 面板回溯 750 日，方案②回退分支切 4 完整窗口）接入两个 energy 作业；② 训练池换池——PX0（2023-09 上市 704 行封顶公共窗口）换出入盲测池、EG0（2018-12 上市 1861 行）换入，训练链 12 品种 SC/FU/BU/PF/TA/EG/L/PP/PG/MA/UR/SA，盲测池自动派生含 PX0；③ 测试：test_walk_forward 新增 500→1 窗/700→4 窗、test_universe_config/test_energy_chain 换池断言更新、jobs 面板常量测试；真实数据验证 days=700 下 fut_wma_cross 走航 n_windows=4/ic_consistency=0.5 通过；**v2.104.0+107 方案②根治断崖**——`_build_wf_config`（AuditPipeline+FuturesEvolutionLoop）≥3 年分支数据不足（<1368 行）自动回退短窗配置，750+ 行不再 0 窗口，消除对 700 魔法值躲断崖依赖；test_evolution_loop +3（默认/回退/750 端到端 n_windows≥2）） |
| GAP-132 | `fts/factor_engine/factor_db/lineage.py` `get_evaluation_trend` + `fts/scheduler/jobs.py` `factor_inspector_job` + `factor_evaluations` 数据积累 | 退化检测因评估历史不足静默失效（数据/机制缺口）：`get_evaluation_trend` 要求 ≥2 条含 `level_1_sharpe` 的评估记录才计算趋势，能化链库 `factor_evaluations` 每因子仅 1 条（311 条，2026-08-14 23:33-23:42 批量写入，FACTORS_WITH_2PLUS_EVALS=0）、期货库 386 条仅 45 因子 ≥2 条 → 全部返回 `insufficient_data`，`detect_degradation` 恒 False | 因子退化安全机制形同虚设：每日巡检静默失效（2026-08-17 实测能化链 242 个 active elite 全量"0 退化"系假阴性），退化因子无法被识别与降级，L3/组合持续消费衰减因子 | 1 月内（评估多期积累后收口） | 🔴 开放（2026-08-17 登记：`factor_inspector_job` 已改默认能化链 + dry-run 防误降级；评估流水多期累积、趋势检测恢复后重审关闭） |
| GAP-130 | `scripts/futures_signal_pipeline.py` `_compute_holdout_validation` + `_compute_per_variety_ic_matrix` | 盲测/IC 矩阵对新上市品种信号-收益错位：收盘价 `df.reindex(common_dates)` 正确对齐共同日期（头部 NaN），但因子信号 `np.array(arr)` 仍按品种自身索引、仅尾部 `np.pad` 补零——品种历史是共同日期尾部子集时，信号与收益错位量 = 品种上市日距共同日起点的缺失天数。2026-08-16 能化链盲测实测：BZ0 错位 32 天 / PL0 错位 42 天 → 盲测 IC 被稀释至 ≈0（-0.0085/-0.0080）误判失效；PR0 错位 2 天 IC 由正确对齐 -0.373 衰减至 -0.134；头部缺口 0 的 16 品种无影响（复算逐位一致）；修正对齐后 BZ0/PL0 原始 IC=-0.20/-0.18，落入训练池区间 [-0.17,-0.43] | 新上市品种盲测 IC/化工链分层泛化/品种-因子有效性矩阵被系统污染误判失效；品种级权重与方向翻转（per_variety_sign_flips）基于被污染的 IC（部分因子符号反转）；报告统计指标（盲测 IC/保持率/分层泛化）含伪影 | 1 周内 | ✅ 已关闭（v2.104.0+81：① 两函数因子信号改经 `align_signal_to_dates`（`df.index.get_indexer` 向量化对齐共同日期，缺失留 NaN，复用 fts/factor_engine/l3_signal_service.py 既有 helper）替代尾部补零——全历史品种（len(sig)==len(closes)）逐位不变零漂移，新上市品种正确对齐；② 盲测池最小真实历史门槛 `ENERGY_CHAIN_MIN_HOLDOUT_ROWS=250`（config/futures_universe.yaml SSOT）；新增 TestHoldoutValidationAlignment +4 用例全绿；重跑 energy 链管线验证 BZ0/PL0 盲测 IC 回升至正常区间） |
| GAP-129 | `tests/`（test_evolution_loop 等 107 处 `EvolutionLoop(` 调用） + `fts/factor_engine/factor_db/schema.py` `get_db_path` | 测试组写真实因子库（测试污染生产 SSOT）：`EvolutionLoop`/各仓储未显式传 `factor_db_path` 时经 `get_db_path(market)` 路由到真实 `data/factor_catalog_{futures,energy}.duckdb`，全库 107 处 `EvolutionLoop(` 测试调用仅 9 处传 `factor_db_path` 隔离（GAP-030 注入机制已存在未被统一采用）——集成测试运行即向真实库写入 factor_catalog/factor_quality_scores/factor_audit_reports（2026-08-16 GAP-128 修复期间实测真实 futures 库出现 `unknown`/`audit_test_factor_001` 测试伪影行） | 测试数据污染 L3 因子资产库 SSOT，可被 L3/信号管线/质检看板误消费（同名因子覆盖/伪影行/统计失真）；与 GAP-128 落库后问题放大（质检行也随测试写入） | 1 周内 | ✅ 已关闭（v2.104.0+79，方案 plans/43 实施：① 根 `tests/conftest.py` autouse fixture 于单挂载点 `fts.factor_engine.factor_db.schema.get_db_path` 全局重定向至每测试独立 tmp DuckDB（4 仓储类构造时局部 `from .schema import get_db_path`，调用时解析模块符号，替换即全量生效；futures/energy 分库文件名保留仅位置迁移，仓储连接自动 init_database 幂等建表）；② `uses_real_factor_db` 标记注册（pytest_configure）+ 5 处豁免——真实路由断言（test_energy_chain/test_factor_db/test_cli_extra/test_evolution_loop）+ 真实存量因子代码数据依赖（test_bincount_boundary 模块级 `_DB_PATH` fixture 前求值无法隔离，read_only 读取无污染）；③ 新增 `tests/test_factor_db_isolation.py` 4 用例（get_db_path 落 tmp + 分库路由保留 / 4 仓储 `_db_path` 落 tmp / 晋升写入三表后真实库 COUNT 不变 / 豁免标记仍路由真实路径）；④ 零污染实测——`pytest tests/factor_engine/test_evolution_loop.py -m "not slow"` 242 passed 后真实库 futures factor_catalog=343/quality=105/audit=103、energy 307/306/307 三表 COUNT 与运行前基线完全一致；受影响回归 factor_db 目录 + test_factor_db + test_cli_extra 219 passed、隔离/豁免 7 passed、energy_chain+bincount 286 passed、ruff 全绿） |
| GAP-128 | `fts/factor_engine/evolution_promote.py` `_write_to_duckdb` + `factor_db/repository.py` | 质检结果未落库（SSOT 一致性缺陷）：晋升管线 `_write_to_duckdb` 仅把 `quality_score`/`audit_report` 内嵌 `factor_catalog.metadata`（JSON 字段），`FactorQualityScoreRepository.save_score`/`FactorAuditReportRepository.save_report` 存在且有单测但**全库零生产调用方** → `factor_quality_scores`/`factor_audit_reports` 专属表恒空（2026-08-16 能化链 106 新精英 JSON 内嵌 audit 106/106 passed、评分 A 34/B 72，DB 两表 0 条；全市场存量同缺） | 质检记录无法经 DuckDB 查询/排序/血缘追踪，`energy_chain_degradation_dryrun.py` C 路（血缘表）跳过，`fts catalog verify` 无法校验质检完整性；违背 L3 因子资产库一数一源 SSOT | 1 周内 | ✅ 已关闭（v2.104.0+78：① 管线治本——`_write_to_duckdb` 落库后追加 `save_score`/`save_report`（非阻塞失败仅告警，写前按 factor_id 清理旧行保幂等，market 自动路由）；② 存量治标——新增 `scripts/backfill_factor_quality_audit.py` 全市场幂等回填并实测；③ 测试 +7 用例全绿） |
| GAP-125 | `scripts/futures_signal_pipeline.py` `_compute_composite_scores` + `_REGIME_FACTOR_SCALE` + 硬编码 `>0/<0` 阈值 + `fts/factor_engine/regime.py` `_build_sector_ohlcv` + `_detect_by_rule` | 信号管道两个 P0 缺陷：① 品种级权重用 `abs(IC)` 丢弃 IC 方向符号 → 反向因子（能源链 3 因子中 `fut_ma_crossover_simplified` 20/20 品种 IC 全负，均值 −0.36）的正信号被当作正贡献累加 → 20 品种综合得分全部为正，bear 主制度下做空信号永不触发（2026-08-16 实测 20 多 / 0 空，"bear 顺势做空但全是做多建议"）；② 主制度对最终输出实际影响为 0——`_classify_factor_category` 将 3 因子全部归类 neutral（关键词表无 trend/ma 命中），`_REGIME_FACTOR_SCALE` 中 neutral 全制度缩放系数 =1.00，同系数缩放归一化后权重比例恒等，bear/bull/oscillate 三组 `--force-regime` 实测输出 0.0001 精度完全一致 | bear 主制度下输出全品种做多且无空头（方向策略语义颠倒）；Regime 检测 300s 计算但对下游无影响（名义功能被旁路）；sector 合成指数为高价品种主导、ADX 恒 0 | 1 周内 | ✅ 已关闭（v2.104.0+65，P0+P1+P2 五修复）：① **IC 方向修复**——`_compute_composite_scores` 新增 `per_variety_sign_flips`（按品种×因子 IC 符号构造 +1/−1，合成层信号乘符号），能源链 21 个 (因子×品种) 组合 IC<0 信号符号反转；② **Regime 阈值**——新增 `_REGIME_SIGNAL_THRESHOLDS`（bear 做多>0.40/做空<−0.15，bull 反之，5 制度差异化）替换硬编码 `>0/<0`；③ **Regime 方向偏移**——新增 `_apply_regime_direction_bias`（bear 得分 ×(1−bias)、bull ×(1+bias)，bias=0.3×confidence）；④ **P1-1 sector 合成指数**——`_build_sector_ohlcv` 改等权收益率指数（各品种归一化首值=100 后截面均值）+ high/low 用截面 max/min 恢复真实波幅（ADX 复活）；⑤ **P1-2 软投票**——`_compute_primary_regime` 改为品种数×sector 置信度软票 + 剔除"炼化聚酯链"训练池并集身份分组防重复计票（未剔除对照 74.84%→剔除后 59.88%）。修复效果：bear 主制度下 0 多/10 空（修复前 20 多/0 空），Regime 置信度越高多空倾向越明显；`--force-regime` 参数新增并保留（验证用）。测试：test_regime/test_sector_regime 102 用例 + test_futures_signal_pipeline/test_energy_chain 57 用例全绿） |
| GAP-123 | `fts/factor_engine/meta_loop.py` `_bootstrap_with_llm`/`L1Verifier.check`/`_try_fix_economic_logic` + `fts/llm.py` `_build_bootstrap_prompt`/`fix_economic_logic` | L1 候选经济逻辑论证质量不足：`economic_logic` 四维评分与 narrative 论证脱节——部分维度 ≥3 分但仅靠抽象套话凑分（"liquidity may drop"、"limited"），无具体机制路径；L1Verifier 只数"维度 ≥3 的个数"不校验"分数与论证匹配"，存在凑分漏洞。2026-08-15 能化链 L1 实测：7 个淘汰因子中 3 个死于"经济逻辑达标维度 1/4 < 2"（42.9%），通用 L1 同类因子（fut_network_stress_tsi 行为 2 分、fut_calibration_anchor 机构 2 分等）存在相同"高分低论证"模式 | 达标维度靠凑分通过/误杀并存：LLM 候选叙事质量低则 L1 通过率受限、高质量因子因维度论证缺失被误拒；narrative 与评分不一致导致后续解释性与审计质量下降 | 1 周内 | ✅ 已关闭（2026-08-17 收尾，方案 4 项全部落地：**P0① bootstrap prompt 强化**（v2.104.0+45：`_build_bootstrap_prompt` 新增「论证-评分一致性」强制规则——维度 ≥3 分必须含具体机制路径 + 反例警示 + 四维机制示例库 + 生成后自检；test_llm +2 用例）；**P1③ 失败重生成反馈闭环**（v2.104.0+46：`LLMClient.fix_economic_logic` 基类默认 None + OpenAIClient/MockLLMClient 实现 + MetaLoop `_try_fix_economic_logic` 软失败分支接入，重写后重新验证通过即注入；test_meta_loop +6）；**P1② 提取器论证补强**——`extractors/base.py` `_ensure_narrative`（plans/44 C3 narrative 补全：LLM 输出 narrative <20 字用「因子名+机制要点」模板补全，零额外 LLM 调用，评分量规明确「3-5 分=有明确机制支撑并在 narrative 中论证」，软失败不因长度被拒；test_base 断言同步 narrative 非空）；**P2④ Verifier 一致性检查**——`meta_loop.py` L1Verifier 校验「分数与论证匹配」而非仅数维度个数（`contracts.require_argument_consistency` 论证-评分一致性强制），凑分漏洞关闭；2026-08-15 能化链 L1 实测 42.9% 死于经济逻辑的占比经提示词/提取器/重生成三层修正后通道打通，高分低论证因子不再侥幸通过） |
| GAP-122 | `fts/factor_engine/portfolio_loop.py` Step 6 + `config/settings.yaml` | L3 Verifier 判定口径错配：① Verifier min_sharpe=2.0 校验的是风控后净暴露 `combo_sharpe`（=signal_sharpe×exposure_scale），而 Regime 降仓×G1 同向敞口压缩（exposure_scale=0.2686）把组合夏普从 1.9782 压到 0.54 → 风控一启用即恒不达 2.0（期货 08-13 五次 + 能源 08-15 两轮全部 verifier_warning）；② max_turnover=0.50 为比例量纲，与因子月度换手次/月量纲（能源精英池 4.48~17.40、等权组合均值 11.60）不匹配，任何因子池选择均无法通过 | L3 组合每日运行恒 verifier_warning（质量门槛误报风控缩放为质量缺陷），阈值与量纲错配导致「调整因子池/风控参数」均无法通过校验 | 1 周内 | ✅ 已关闭（v2.104.0+42：Step 6 新增 `_verifier_view` 用缩放前 `signal_sharpe` 替换 `combo_sharpe` 判定 min/max_sharpe——风控缩放为暴露决策非质量判定，与「双指标」设计语义一致；settings.yaml verifier `min_sharpe 2.0→1.9`（2.0 已被 SHARPE_CAP=2.0 截断的因子等权合成贴线）、`max_turnover 0.5→12.0`（次/月量纲）；test_portfolio_loop.py +4 用例（视图替换/缺失原样/风控缩放组合经视图通过/低信号质量仍失败）；test_portfolio_loop.py 227 全绿 + ruff 通过） |
| GAP-118 | `seeds/futures/{alpha_behavior,market_regime,operator_dict}.yaml` | 种子因子缺 params 字段：`scripts/unified_factor_converter.validate_all` 必填字段含 `params`（REQUIRED_FIELDS），但 27 个种子因子（alpha_behavior.yaml fut_gp_alpha1 / market_regime.yaml fut_macro_{cpi,interest_rate,export,import,us_bond} / operator_dict.yaml 21 个量价种子）定义中无 `params` 键 | `fts seed validate` 退出码 1（27 因子「缺少必填字段: params」），WorkFlow 端到端 s3 预处理阶段被阻断，依赖种子的演化/信号链路无法启动 | 1 周内 | ✅ 已关闭（v2.104.0+25：按既有 `params:` 风格为 27 个因子补 `params: {}`（函数内 `params.get('window', 默认)` 消费不受影响），`fts seed validate` 恢复「所有因子验证通过 + 无跨文件重复」全绿，WorkFlow s3 端到端解锁） |
| GAP-117 | `scripts/gap_threshold_calibration.py` | 阈值校准脚本日换手反推口径错误：`turnover_monthly/21` 反推日换手，但库内月度换手 = 日换手 × 42（G11 信号翻转率口径：`turnover_daily = mean(\|Δsign\|)/2`、`turnover_monthly = mean(\|Δsign\|)×21`，见 evaluation_chain 时序/横截面两条路径与 `scripts/backfill_turnover.py`）——`/21` 将日换手高估 2 倍（2026-08-14 全库校准实测：日换手 P90=0.324 应为 0.162） | G4/G11 校准分布失真：日换手候选阈值通过率虚高（≤0.20 通过率 80.1% 应为 91.1%），定值依据偏离真实分布；35-gap-closure-plan §9.1 历史口径引用同样错误 | 1 周内 | ✅ 已关闭（v2.104.0+16：`TRADING_DAYS_PER_MONTH=21` 改 `TURNOVER_DAILY_TO_MONTHLY=42` 并修正 docstring 口径说明；新增 test_gap_threshold_calibration.py 7 用例全绿 + ruff 通过；重跑全库校准生成正确基线，修正后与 G11 定值基准 active 因子 P90=0.320 量级一致） |
| GAP-116 | `fts/factor_engine/symbol_holdout.py` | symbol_holdout 留存率判定噪声主导：`retention = holdout_ic / \|train_ic\|` 在弱信号下被近零分母放大——2026-08-14 夜间演化 trace 实测 fut_linearreg train_ic=0.0278/holdout_ic=0.0009 → retention 3.2% 被判 failed（留出 8 品种正负混杂），对照组 fut_trix train_ic=0.0197/holdout_ic=0.0200 → retention 101%，留出集 ±0.005 量级噪声完全决定通过/失败 | 弱信号因子的留出验证结果不可靠（同量级噪声下 retention 3% vs 101%），通过/失败判定失去统计意义 | 1 周内 | ✅ 已关闭（v2.104.0+15：`SymbolHoldoutConfig` 新增 `min_train_ic=0.05`，`run_symbol_holdout()` 训练集 \|IC\| < 阈值返回 None（audit `_check_symbol_holdout` 对 None 自动 skipped，不阻断演化流程），`min_train_ic=0` 回退旧行为向后兼容；测试 test_symbol_holdout.py +3（弱信号返回 None / 阈值 0 向后兼容 / 强信号不受影响）15→18 用例全绿 + ruff 通过） |
| GAP-115 | `fts/factor_engine/evolution_loop.py` + `evolution_uct.py` + `cli.py` | 熔断预算传播缺陷：cli.py 在构造 `EvolutionLoop` 后执行 `loop.budget = budget`（失败率阈值 1.0=禁用、`FTS_EVOLUTION_CB_FAILURE_RATE` env 可覆盖，2026-08-13 用户指令"夜间强制跑满世代数"），但 `EvolutionLoop.budget` 为普通属性，重绑仅改主类字段；熔断检查走 `self._uct_selector._check_circuit_breaker()`，UctSelector 构造时（`evolution_uct.py:45 self.budget = budget`）持有 `DEFAULT_BUDGET_CONFIG`（0.95）引用 → 重绑未传播，夜间演化仍按 0.95 失败率熔断提前终止 | 夜间演化无法跑满 50 代（2026-08-13/14 连续三次实测 g16~g23 熔断），"强制跑满世代数"指令与 env 覆盖失效；与 GAP-114 换手错杀叠加造成稳定 0 晋升 | 1 周内 | ✅ 已关闭（v2.104.0+14：`EvolutionLoop.budget` 改 property setter，重绑时同步 `_uct_selector.budget = value`（唯一经构造注入 budget 的协作类；其余协作类经 `owner.budget` 动态读主类不受影响）；cli env 覆盖、任意重绑入口自动传播；测试 test_evolution_loop.py +3（重绑阈值生效/禁用态 100% 失败率不熔断/重绑保留熔断态按新阈值触发）全绿 + ruff 通过） |
| GAP-114 | `fts/factor_engine/verifier.py` + `contracts.py` | Verifier Level 1 换手绝对阈值错配：`max_turnover_monthly=5.0` 与期货横截面真实换手分布严重脱节——库内 83 个 active 因子中 74 个有真实换手，75.7%（56/74）换手 >5.0、中位数 6.27、p90=15.6；2026-08-14 夜间演化 7 个审计通过的高 IC 因子（换手 8.68~10.85，处库内 p50~p75 正常区间）全部被 Level 1 换手门槛系统性拒绝 → 演化稳定 0 晋升（2026-08-13/14 连续复现）；另口径混乱三处：`contracts.py:VerifierConfig` 注释「默认 0.50」vs 实现 5.0、演化内部 L1 候选缺省换手 0.5、03-configuration 表标 0.50 | 夜间演化产出能力受阻（50 代 0 晋升空转），高 IC 因子因换手「口径错杀」无法入库；阈值与 `factor_turnover_daily_max=0.45`（P95 校准拦 top 5% 极端）分层错位 | 1 周内 | ✅ 已关闭（v2.104.0+13，方案 A：`max_turnover_monthly` 升级为成本敏感净收益校验触发线——换手超 5.0 时按 `净夏普=毛夏普−月换手×12×2×单边成本率/年化波动`（`one_side_cost_rate=0.0005`/`assumed_annual_vol=0.15`/`turnover_cost_net=True`，与 `cost_model` 净夏普口径一致）判定，净夏普 ≥ `min_sharpe` 即准入并记录 `cost_adjusted` 审计明细，否则才判失败；`turnover_cost_net=False` 回退旧绝对硬剔；三处口径统一「次/月」（contracts 注释 0.50→5.0、03-configuration 0.50→5.0、演化内部缺省 0.5 注明单位），与日换手硬剔除分层（外层拦极端/内层成本覆盖判定）；test_verifier.py +5 用例（成本覆盖放行/成本不足拦截/开关回退/明细字段/毛夏普口径）全绿 + ruff 通过） |
| GAP-I306 | `fts/factor_engine/portfolio_loop.py` `_auto_build_factor_returns` | 自动构建因子收益矩阵 Sharpe 虚高：横截面多空（quantile=0.2、25 品种 → 每腿仅 5 品种）构建的收益序列年化 Sharpe 达 20.06（v2.104.0+2 实测），远超 max_sharpe=3.5 过拟合告警线；measured 口径组合夏普被污染，不能作为 Verifier 校验输入 | 若默认启用自动构建，组合指标/Verifier 判定失真（虚高而非真实的 0.54 低估）；当前已默认关闭（env `FTS_L3_AUTO_FACTOR_RETURNS=1` 显式启用），调度回退估算口径不受影响；修复需增大分位腿（quantile 0.3-0.4）/启用方向校准/收益缩尾或波动率下限 | 下一里程碑 | ✅ 已关闭（v2.105.0：`_auto_build_factor_returns` 自动构建因子收益矩阵质量修复三件套——① **分位腿增大**：`FactorReturnsConfig` quantile 由 0.2 上调（多空每腿样本增大，避免 25 品种下每腿仅 5 品种的小样本虚高）；② **方向校准**：按因子 IC 符号校准多空方向，防反向腿错配放大收益；③ **缩尾/波动率下限**：收益序列缩尾 + 波动率下限护栏防极端头寸收益污染；修复后实测 Sharpe 由 20.06 回落到正常量级（不再超 max_sharpe=3.5 过拟合告警线），`measured` 口径组合夏普可作 Verifier 校验输入；配 `FTS_L3_AUTO_FACTOR_RETURNS=1` 显式启用路径；自动构建开关与 `--returns-matrix` 手动路径语义保留） |
| GAP-I307 | `fts/factor_engine/meta_loop.py` `_scan_injected_names` + `evolution_seeds.py`/`evolution_futures.py` `_merge_l1_candidates` + `contracts.py` `FactorPoolEntry` | L1 Step 2.5 去重口径失效：`_scan_injected_names` 以 `l1_injected/` 目录文件为唯一事实源，但该目录会被 L2 演化按 GAP-036 消费后删除（消费后立即删 + 历史遗留清理 + 晋升后删）→ 目录空即永远扫不到历史注入名（实测 08-13：运行 3 扫到 3 个 → 运行 4/5 扫描 0 个，fact_pool 累计 48 个注入名对去重不可见）；附带：evolution 消费后直接 `write_text` 写 factor_pool.json，不重算 total_count/pending_count，顶层字段残留过期值（pending_count=11 而实际 pending=0） | 历史注入名不可见 → L1 重复注入同名因子（冗余 + LLM token 浪费）；去重集合为空使 Step 4 require_nodup 同步失效；pool 顶层计数漂移 | v2.104.0+10 | ✅ 已关闭（v2.104.0+10：① `_scan_injected_names` 改读 factor_pool.json（SSOT 索引，GAP-036 消费后仍保留）——按 `self.market` 过滤，market 缺失的历史记录纳入（宁多勿漏）；`FactorPoolEntry` 契约新增 `market` 字段，L1 注入 entry 写入 `self.market`；② evolution_seeds/evolution_futures 消费后重算 `total_count`/`pending_count`（与 `FactorPoolManager.save` 口径一致）；③ 测试 +3（test_meta_loop 93→95：scan_injected_names 读 pool 按市场过滤/空 pool 空集/run 注入 entry 断言 market；test_evolution_l1_merge 13→14：消费后 pending_count/total_count 重算），模块回归 95+14+5+5 passed 全绿 + ruff 通过） |
| GAP-149 | `fts/factor_engine/factor_db/repository.py`（`update_factor`/`update_factor_status`/`retire_factor` 写入口）+ `factor_catalog_energy.duckdb` | 因子状态枚举缺运行时校验：写入口无 status 枚举约束（合法集 active/degraded/pending/deleted/shadow），2026-08-19 因子级监控实测 energy 库 **158 条非法状态因子**（status 不在合法枚举内，多为历史遗留非标准值） | 非法状态因子绕过 7 状态生命周期机，状态看板/生命周期收口消费异常状态时静默错位；退化检测/巡检按状态过滤会漏检或误分组 | 下个里程碑 | ✅ 已关闭（v2.105.0+19：`VALID_CATALOG_STATUS` 合法集（active/shadow/degraded/pending/failed/deleted/retired）+ 写入口三处枚举校验（create_factor/update_factor/update_factor_status 非法值抛 ValueError）+ `list_invalid_status` 存量库扫描（market 可过滤）；v2.105.0+20 补 archived/deprecated（futures 库生命周期终态：归档/废弃）；**存量"158 条非法状态"确认为监控口径不完整（缺 shadow/archived/deprecated 误报）——修正合法集后 energy/futures 实测非法归零，无需数据处置**；测试 +6） |
| GAP-150 | `fts/store/registry.py`（`StorageRegistry.validate_contract`）+ 各数据域写路径（`data_futures.py`/`factor_db`/`state_db`/`l3_signal_service`） | 存储域契约仅静态校验 YAML 字段，不拦截实际写入：写入库路径/表名由调用方自由指定，无法保证落在 registry 登记域内 → 域漂移/双写只在数据消费侧暴露（GAP-090 "一数一源"写入口无强制） | SSOT 契约在写入口形同虚设，新增写路径绕过注册表后契约失效且不可见 | 下个里程碑 | 🟡 部分（v2.105.0+19：`StorageRegistry.find_by_path` 模板/前缀路径反查 + `warn_unregistered_write` 告警模式 + energy 因子库登记 storage_landscape；v2.105.0+20 严格模式默认开启——`FactorRepository` 默认路径未登记抛 ValueError 阻断（env `FTS_STORAGE_WRITE_STRICT=0` 回退告警），显式注入豁免；其余写入口（信号库/状态库/行情库）接入排期；测试 +8） |
| GAP-151 | `fts/data_futures.py`（`_read_kline_cache` 等数据加载路径）+ 数据契约消费方（逻辑监控/因子执行） | 数据契约字段完整性缺运行时断言：2026-08-19 实测 `hold` 字段 100% 缺失被三层代理兜底（标准化/去重/20 日滚动均量填充）掩盖，加载后无必填字段清单校验 | 必填字段缺失被代理值"合理化"，因子在无真实持仓数据的日子表现"合理但失真"，且缺失不可见（代理兜底是 L3 错误的遮羞布） | 下个里程碑 | 🟡 部分（v2.105.0+19：`KLINE_REQUIRED_FIELDS` 必填字段集 + 加载后完整性告警，接入 `_read_kline_cache` 代理填充前；v2.105.0+20 分级校验——核心字段（date/open/high/low/close/volume）缺失/全空→error+跳过（宁缺毋滥），增强字段（hold/settle/pre_settle）缺失→warning+代理降级显式暴露（hold 全空不再静默）；测试 +4） |
| GAP-169 | `fts/factor_engine/numba_kernels.py`（`@njit` 模块级装饰）+ 环境（numba 与 numpy 版本） | numba 安装但版本不兼容时整模块 import 崩溃：本机 numpy 2.5.2 而 numba 需 numpy≤2.4（`ImportError: Numba needs NumPy 2.4 or less`）→ `except` 捕获后 `njit=None`，模块级 `@njit(...)` 装饰执行 `None(...)` 抛 TypeError，`import numba_kernels` 整体失败，调用方无法走"回退现值"降级（2026-08-20 plans/57 阶段 0 A/B 实测 `ts_zscore` 等 DSL 算子报 `'NoneType' object is not callable`） | 算子 numba 快速路径依赖全部失效且降级通道断裂；DSL 时序算子（ts_zscore/ts_rank/ts_cvar）不可用，信号矩阵构建受影响 | 1 周内 | ✅ 已修复（2026-08-20 plans/57：`except` 分支 `njit` 改透传装饰器（`@njit` 无括号/`@njit(...)` 带括号均返回原函数），模块正常加载，`enabled()=False` → 内核入口返回 None → 调用方回退 pandas 现值，零漂移；`verify_factor_mapping.py` 实测 ts_zscore/ts_realized_vol/ts_ma_diff 等全部恢复。**遗留**：numba 加速本身仍不可用（numpy 需降级至 ≤2.4 或升级 numba，环境决策，性能基准 10x 门槛暂停）） |
| GAP-170 | `fts/factor_engine/factor_program.py`（`_ArrayDataWrapper` + 沙箱回退）+ 环境（pandas 3.0.5） | pandas 3.0 `np.asarray(Series, dtype=float)` 返回只读数组（`writeable=False`）：legacy Python 模板因子（`def factor_program`）中 `a = np.asarray(s, dtype=float)` 后原地赋值 `a[np.isnan(a)] = 0.0` 抛 `ValueError: assignment destination is read-only`；三层回退（wrapped→raw df→dict）全部失败 → `FactorCompileError` → 信号留 NaN（2026-08-20 实测 `test_matches_per_symbol_execution`/`test_signal_cache_shared` 预存失败） | legacy Python 模板因子路径失效（仅 operator/DSL 因子不受影响——已实测 `ts_*` DSL 正常）；影响存量手写模板因子与对应测试 | 下个里程碑 | 🔴 开放（2026-08-20 登记：受影响的 `_mk_factor` 模板因子；operator kind（DSL 算子）路径不受影响——`_execute_operator` 走解析执行不经 Python 模板；修复方向：`factor_program` 回退路径对 ndarray 显式 `.copy()` 或模板 `np.asarray(..., dtype=float).copy()`，待专项） |
| GAP-171 | `D:\Regime-Driven\src\regime_system\strategy_synthesis.py`（`ml_ensemble_weights`）+ 环境（lightgbm 4.6.0） | lightgbm 4.6 小截面零分裂：默认 `min_child_samples=20` 使 ≤20 样本截面无法分裂，`feature_importances_` 全 0（split/gain 均零）→ ML 集成权重恒等权回退（2026-08-20 plans/57 §2.3 迁移实测 20 品种合成面板） | ML 集成合成模式在中小截面（子链 3~20 品种）无信号区分度，退化等权 | 1 周内 | ✅ 已修复（2026-08-20 plans/57：`importance_type="gain"` + `min_child_samples=5`，小截面可分裂；`test_strategy_synthesis.py` 20 用例全绿） |
| GAP-172 | `fts/store/registry.py`（`warn_unregistered_write` 严格模式）+ `tests/conftest.py`（GAP-129 测试隔离 get_db_path 重定向 tmp） | GAP-150 严格写路径注册与 GAP-129 测试隔离冲突：隔离 fixture 将 get_db_path 重定向至 pytest tmp（项目根外），`find_by_path` 对项目根外返回 None（注释"显式注入豁免"），但 `warn_unregistered_write` 严格模式把项目根外路径也当"未登记"抛 ValueError → 评审/自动审核/模拟审批等 22 测试文件全量回归 48 failed + 22 errors（2026-08-20 全量回归实测 8079 passed / 48 failed / 22 errors，单测隔离可复现） | 严格模式误伤测试隔离路径，阶段 2 退役里程碑全量回归门禁被预存失败阻塞 | 1 周内 | ✅ 已修复（2026-08-20 plans/57：`warn_unregistered_write` 对项目根外路径（`Path.resolve().relative_to(PROJECT_ROOT)` 失败）豁免严格校验（保留告警模式语义）；`test_storage_registry.py` 严格模式断言改为项目根内未登记路径 + 新增 `test_strict_outside_root_exempt`；受影响测试 70 用例全绿） |
| GAP-173 | `fts/factor_engine/portfolio_loop.py`（signal_sharpe measured/estimated 语义）+ `tests/factor_engine/test_portfolio_{loop,qc,walk_forward}.py` + `test_regime_features.py` + 环境（pandas 3.0.5） | 全量回归残留预存失败 21 项（2026-08-20 全量回归实测 8129 passed / 21 failed，GAP-172 与 universe 测试修复后）：① portfolio_loop signal_sharpe 断言 `estimated` ≠ `measured`（工作区既有未提交本地改动 194 行引入，非本次计划改动）；② `test_portfolio_walk_forward` 5 项 `ValueError: underlying array is read-only`（pandas 3.0 `np.asarray(Series)` 只读，GAP-170 同类）；③ `test_regime_features` 常数序列 cross_symbol_correlation nan vs 0.0 | 阶段 2 退役里程碑全量回归门禁仍有预存失败（与 plans/57 无关，属工作区既有状态与环境兼容） | 下个里程碑 | 🔴 开放（2026-08-20 登记：非 plans/57 引入；`git diff` 确认 portfolio_loop/test_portfolio_loop 为会话前既有本地改动 194/168 行；pandas-3 只读归并 GAP-170 专项） |
| GAP-161 | `fts/factor_engine/regime_thresholds.py`（新增）+ `factor_inspector.py`（`AutoReviewPolicy.classify`）+ `ir_thresholds.py` + `qa/monthly_check.py` + `qa/quarterly_check.py`（F5） | 阈值静态化（plans/59 OPT-01）：IC/Sharpe/IR 门槛全局单值，未按市场 regime（趋势/震荡/高波动）分层；F5 只"标记条件 IC 变化"不"调门槛"，regime 敏感因子判定失真 | 震荡市与趋势市同一标准，regime 敏感因子要么过严要么过松 | 1 月内 | ✅ 已关闭（2026-08-20 v3.0.0+18 plans/59 OPT-01 实施：新增 `regime_thresholds.py`（`RegimeThresholdConfig` + `apply_regime_multiplier` 乘数查表，regime 归一 bull/bear→trend）→ `AutoReviewPolicy.classify`（min_ic/min_sharpe）/ `ir_thresholds.factor_ir_threshold`（min_ir）/ `monthly_check` M2 健康下限 / `quarterly_check` F5 门槛调整告警 四接入点；默认 enabled=False + 乘数全 1.0 向后兼容，`FTS_REGIME_THRESHOLDS_ENABLED` 显式开启；新增 test_regime_thresholds.py 19 用例 + 受影响 111 用例全绿 + ruff 通过） |
| GAP-162 | `fts/factor_engine/evolution_promote.py`（`_promote_to_elite` 多重检验门）+ `verifier.py` + `factor_db/repository.py` | 跨运行累积 FDR 无控制（plans/59 OPT-02）：Bonferroni 只控单次评估批次，因子跨日多次重试后"最终通过"与"首次通过"假阳性风险不等价，无 discovery discount | 长周期运行下"重试碰巧生效"因子混入 elite，红线"多重检验校正"在时间维度失效 | 1 月内 | ✅ 已关闭（2026-08-20 v3.0.0+20 plans/59 OPT-02 实施：新增 `fdr_discount.py`（`FdrDiscountConfig` + `apply_fdr_discount`：p_eff = min(1.0, p × discount^retries)，retries cap 10 防指数爆炸）→ `evolution_promote._promote_to_elite` 多重检验门接入（重试次数取 `repo.get_evaluations(factor_id)` 历史评估数，p_eff > alpha 拦截）；默认 enabled=False 向后兼容，`FTS_FDR_DISCOUNT_ENABLED`/`FTS_FDR_DISCOUNT`/`FTS_FDR_ALPHA` 显式开启；新增 test_fdr_discount.py 14 用例 + test_evolution_promote_gap128 回归全绿 + ruff 通过） |
| GAP-163 | `fts/factor_engine/scope_domain/guard.py` + `scope_domain/hooks.py` + `subchain_profile.py` + `factor_inspector.py` | 特异因子无 OOS 前瞻二期确认（plans/59 OPT-03）：子链/品种特异"一次判定终身画像"，护栏仅"宁漏标不误标"，无晋升后真实样本外二次确认；单链 n≥3 / 单品种 500 交易日硬门槛牺牲真实次新/双品种特异 | 特异画像一旦误标长期影响组合；真实特异因子可能被硬门槛误杀 | 1 月内 | ✅ 已关闭（2026-08-20 v3.0.0+21 plans/59 OPT-03 实施：新增 `specific_observe.py`（`SpecificObserveConfig` + `build_observe_marker`/`observe_phase`/`shrink_scope_ic`（n<3 贝叶斯收缩）/`review_specific_oos`（confirm≥confirm_min_ic / revoke 衰减>revoke_ic_decay / hold 顺延））→ `evolution_promote._write_to_duckdb` 晋升特异因子（domain_stats.scope.kind ∈ symbol/chain）写 `metadata.specific_observe` 观察期标记 → `FactorReviewWorkflow.review_specific_observations` 扫描观察期满因子复核（confirm 固化/revoke 清画像+留痕 specific_revoke/hold 顺延 grace，ic_provider 依赖注入数据不可得宁缺毋滥）；默认开启（`FTS_SPECIFIC_OBSERVE_ENABLED`）；新增 test_specific_observe.py 20 用例 + test_qa_subchain/test_lifecycle_subchain/test_factor_inspector 回归 72 用例全绿 + ruff 通过） |
| GAP-164 | `fts/factor_engine/factor_quality_card.py` + `high_ic_screener.py`（ic_mean）+ `qa/pre_entry.py`（Q4）+ `evolution_promote.py`（Verifier）+ `factor_inspector.py` | IC 类判定多处重复无差异告警（plans/59 OPT-04）：Verifier/B.4 ic_mean/评分卡 ic_score/Q4 四处判定同一信号，一处口径漂移全链级联，无多口径差异可见性 | 口径漂移静默传播，评审结论一致性不可观测 | 1 月内 | ✅ 已关闭（2026-08-20 v3.0.0+22 plans/59 OPT-04 实施：新增 `qa/ic_consistency.py`（`check_ic_consistency` 纯函数——主表 `catalog.ic` 为权威口径，其余来源偏差 > 容差 0.005 标记不一致，有效来源<2/权威缺失不误报）→ `FactorReviewWorkflow.review_inplace` 接入（对比主表 ic vs `factor_evaluations.level_1_ic` 最新评估，漂移 > 容差 → 撤销 review 转人审，宁缺毋滥不静默通过；校验异常回退正常评审）；口径收敛语义：IC 类判定（Verifier/B.4/评分卡/Q4）统一以评估链 `level_1_backtest.ic` 为单一事实源，主表与评估历史脱节由本校验暴露；新增 test_ic_consistency.py 12 用例 + qa/factor_inspector/auto_review 回归 124 用例全绿 + ruff 通过） |
| GAP-165 | `fts/monitor/data_quality_monitor.py` + `fts/factor_engine/factor_inspector.py`（评审作业）+ `qa/report_template.py` | 数据质量与评审割裂（plans/59 OPT-05）：`data_quality_monitor`（缺失率/异常值/多源分歧）独立运行，因子评审不感知当期数据质量，数据异常期做出的 approved 会污染评审历史 | 数据异常期评审结论失真且无标记 | 1 月内 | ✅ 已关闭（2026-08-20 v3.0.0+23 plans/59 OPT-05 实施：新增 `qa/data_gate.py`（`assess_data_quality` 三态评估——缺失率/异常值/多源分歧超严重线 → critical；`data_gate_decision` critical→defer）+ `FactorReviewWorkflow.review_inplace` 接入（`data_quality_provider` 依赖注入取当期数据质量快照，approved 前数据严重异常 → 标记 data_degraded 暂不 approved 延迟下期，reject 路径不受影响；provider 缺失不误伤）；阈值对齐 data_level_monitor；新增 test_data_gate.py 12 用例 + qa/factor_inspector/auto_review 回归 151 用例全绿 + ruff 通过） |
| GAP-166 | `fts/factor_engine/factor_inspector.py`（needs_human/pending）+ `qa/status_board.py` + `fts/scheduler/jobs.py` | 人审无超时 SLA（plans/59 OPT-06）：needs_human / D1 逻辑复审依赖人工且无超时路径，pending 可长期堆积 | 待审因子无限滞留，L3 池供给受人工节奏制约 | 1 月内 | ✅ 已关闭（2026-08-20 v3.0.0+23 plans/59 OPT-06 实施：新增 `qa/review_sla.py`（`sla_phase` active/warned/escalated 三阶段 + `sla_marker` 权重缩放标记）→ `FactorReviewWorkflow.enforce_review_sla` 全程机审（扫描 pending 因子按 created_at 判定——超 sla_days 降权 50%（metadata.review_sla）→ 超 escalation_days 退 L2 冷却池（status=degraded + factor_reviews 写 rejected 留痕，evolution_seeds 30 日冷却后回归）；已处置防重复；dry-run 支持）；默认开启（`FTS_REVIEW_SLA_ENABLED`）；新增 test_review_sla.py 15 用例 + qa/factor_inspector/auto_review 回归 151 用例全绿 + ruff 通过） |

### P2 — 一般改进（优化代码质量）

| ID | 模块 | 差距描述 | 影响 | 处理期限 | 状态 |
|:---|:-----|:---------|:-----|:---------|:-----|
| GAP-089 | `fts/data_sources/tqsdk_tick_source.py` + `fts/data_sources/migrate.py` | 高频数据深度不足：tick_cache 32 列仅 5 档盘口且 TQSDK 实测仅 ~42 分钟样本（跨会话增量累积中）；无逐笔委托/成交（order/execution）数据、无 10 档深度、无订单簿重建；分钟级 11 列无持仓/结算 | 微观结构因子（OFI/OBI/大单占比已建，C1）受数据深度限制，高频因子体系无法对标头部机构（订单不平衡/撤单率/暗池） | 3 月内 | 🟡 开放（**受限登记（2026-08-12）**：数据深度受外部数据源制约——TQSDK 行情深度与历史样本由天勤/交易所数据权限决定，逐笔/10 档/订单簿重建非本机可实现；跨会话 tick_cache 增量累积（GAP-I503）已落地作为样本积累通道，剩余深度扩展待数据源条件成熟，**登记受限不假完成**） |
| GAP-090 | 全链路数据层（`fts/store/` 新增）+ `docs/harness/_data/storage_landscape.yaml` + `scripts/migrate_elite_json_to_catalog.py` | 数据持久化治理差距：数据格式分散（8+ 种形态并存：duckdb/json/npy/jsonl/csv/png/gz/sqlite）、JSON 泛滥（3,670 个文件、22.7MB，其中演化 traces 1,494、combo_history 200、agent_proposals 370）、同类数据双写漂移（elite 因子 JSON 快照 + DuckDB catalog 双写 GAP-032 反向；factor_catalog 拆 3 库却每库重复建 13 张表）、信号缓存裸 .npy 无 schema/校验（283 个）、`fts_history.duckdb` 单写者连接被进程长期持有（只读亦被锁） | 数据管理/持久化无统一目录（SSOT 未达成）；状态类（可重建）与资产类（不可重建）混放；迁移无渐进通道 | 3 月内（plans/29 P0~P4） | 🟡 开放（**P0 基建 ✅ + P1 因子资产入库 ✅（2026-08-11）**：P0——新建 `fts/store/` 存储域注册表（`StorageRegistry`/`StorageDomain`/`StorageBackend` 枚举 + YAML 契约加载与校验，13 域登记于 `docs/harness/_data/storage_landscape.yaml`）；`FTS_STORAGE_LANDSCAPE_PATH` env 覆盖；tests/store 13 用例全绿；零存量数据变更。**P1**——新建 `scripts/migrate_elite_json_to_catalog.py`（差量补齐+逐字段校验+dry-run/verify-only/`--sync`，trace_id 贯穿，幂等可重入）：stock 补齐 389（525 行）、futures 补齐 139（326 行），**差量缺失归零**；`--sync` 同步既有漂移 83 处（JSON 含 NaN 加固更新于 catalog 之后，以 JSON 为准同步内容字段）；**778 因子逐字段校验 0 不一致**（`--verify-only` 复核 migrated=0/mismatches=0）；写路径反转 `_promote_to_elite`（先 DuckDB 后 JSON 快照，JSON 降级只读备份）；`add_evaluation` 新增 `update_catalog_status` 参数（迁移归档因子避免 archived→active 覆盖）；迁移前备份 `data/backup/factor_catalog_{stock,futures}.duckdb.bak.p1_20260811`；tests/scripts 17 用例全绿；受影响回归 evolution_loop promote 105 + factor_db 67 passed。**P2（2026-08-11）**——新建 `fts/store/state_db.py` `StateKVStore`（`state_kv` 当前状态表 namespace+key 复合主键 UPSERT + `state_history` 历史追加表 seq 自增可回放，默认库 `data/state.duckdb`）+ `scripts/migrate_state_to_duckdb.py`（权威状态 glob 规则入库 + 过程痕迹 tar.gz 归档复制语义 + dry-run/verify-only，trace_id 贯穿，幂等可重入）：**231 权威状态条目全部入库**（`state_kv` 231 行：portfolio 225 + evolution 2 + extractors 1 + knowledge 1 + loop 1 + meta_loop 1；`state_history` 231 行），读回逐字段对账 **231/231 一致 0 mismatch**，SQL 级抽样复核 combo_history 嵌套完整；过程痕迹 **2307 文件归档** `data/archive/state_traces_20260811235839.tar.gz`（源保留，删除留 P4）；`StateKVStore.snapshot()` 支持无 state.json 冷启动；tests/store 11 + tests/scripts 8 = **19 用例全绿**，受影响回归 `pytest tests/store/ -v` + ruff/mypy 全绿。**P3-A（2026-08-11）**——`fts/factor_engine/factor_optimizer.py` `FactorSignalCache` 信号缓存 Parquet 化：`put` 写单列 Parquet（**DuckDB 原生写/读，零新增依赖**）+ `signal_index.json` 元数据表含 backend/version/checksum；`get` Parquet 优先校验 checksum（篡改判 miss 删除）、`.npy` 只读兼容回退并自动重建；`clear`/`invalidate_factor` 双格式清理；test_factor_optimizer **51 passed**（+5 Parquet 用例）+ ruff/mypy 全绿；storage_landscape signal_parquet 域升 active。**P3-B（2026-08-12）**——行情库冷热归档：新建 `scripts/archive_history_cold.py`（dry-run/verify-only/archive，trace_id 贯穿，幂等，写锁占用降级拒绝）；演化任务结束后无写锁窗口实际执行——`kline_cache` **≤2013 共 44,134 行（2005-2013，9 年）导出 `data/archive/history_kline_cache_{year}.parquet`（1.22MB）并从热库 DELETE**，热库剩余 323,839 行（2014-2026 完整），verify 一致（cold_rows=44134/hot_remaining=0）；归档前备份 `data/backup/fts_history.duckdb.bak.p3_20260812`；test_archive_history_cold **7 用例全绿** + ruff/mypy 全绿；storage_landscape market_history 域标注冷热分层。**连接生命周期治理**：`data_futures.py` 全局单写者演化进程存活期持写锁（连只读亦锁），通过「脚本化冷热归档 + 无写锁窗口执行」缓解，底层改造登记远期遗留。**P4（2026-08-12）部分实施**——storage_landscape 终态刷新（8 active + 5 legacy 标注待冻结期退役）、一致性 13/13、P0~P3 受影响回归 90 passed；**清理项（elite JSON 快照/状态 JSON/.npy/experiments）受计划「冻结期≥1 发布周期」约束，刚完成迁移冻结期未满，登记待冻结期满执行**（bump 亦留待冻结期正式发布）。全部计划推进完毕（P0~P4，P4 清理项待冻结期）。**P4 读路径切换（2026-08-12）**——JSON 双读兼容期收紧为 DuckDB SSOT 优先、JSON 仅回退：(a) 动态池 `get_dynamic_core_subset` SSOT `state.duckdb`（portfolio/futures_dynamic_pool）→ JSON 缓存 → 静态池三级降级 + sync_liquidity_pool 写路径同步；(b) CLI `factor list/show` 默认 DuckDB 查询、JSON 目录仅回退、`factor show` 补 `--market`；(c) 运行状态 state.py/portfolio_loop/meta_loop 读写 → `StateKVStore`（get_state_store 进程级单例 + 可注入 store）；(d) extractors base `_load_state`/`_save_state` → `StateKVStore`。测试适配（状态类注入临时 StateKVStore fixture 隔离 SSOT；test_dynamic_pool _EmptySSOT mock；test_cli_extra 目录模式用例模拟 DuckDB 不可用回退）。定向回归全绿（store 24 + migrate_state 8 + archive_history 7 + factor_optimizer 51 + dynamic_pool 10 + cli 264 + 状态套件 634）+ ruff/mypy 全绿。**E.3 S2 连接生命周期 L4 侧根治（2026-08-13）**——`state.duckdb` 后端切换 **SQLite WAL**（`data/state.db`）：WAL 多读单写不互斥，演化进程写连接存活期间外部只读不再被锁（对照：DuckDB 实测 read_only 报 File is already open）；upsert 单事务双表原子、seq AUTOINCREMENT 单调；`scripts/migrate_state_to_sqlite.py` 迁移（幂等/锁占用降级/不删源冻结期）；tests 33 passed + 调用方 74 passed + ruff/mypy 全绿；storage_landscape run_state 域 backend=sqlite。**L2/L3 DuckDB 库连接生命周期根治（S1：写连接短生命周期 + filelock 跨进程写互斥，2026-08-13 实施）**——见 design/E.2 推荐路线，现已实施：① 新增 `fts/store/duckdb_lock.py` 跨进程写锁（msvcrt/fcntl 标准库零依赖，`data/.locks/{name}.lock`，超时抛 TimeoutError）+ 4 用例；② L2 `data_futures.py` 删 `_WRITER`/`_DB` 全局常驻缓存，新增 `_write_scope()`（filelock+短连接），读池改 `read_only=True`，`_write_contract_kline` 迁移短连接；③ L2 `aggregator.py` 删 `_cache_conn` 常驻连接，读路径 `_open_read_conn`（read_only 短连接）+ 写路径 `_write_scope`；④ L3 `repository.py` 4 类 `_get_conn` 补 `lock_configuration=true` + 嵌套 repo 用后 close；⑤ 演化晋升 `@_release_repo_after` 方法退出即释放 repo 写锁；⑥ 同步脚本写段迁移；⑦ 受影响模块 **653 passed** + ruff 全绿 + mypy 通过（剩 2 处 HEAD 预存不顺手修）；顺带修复预存 Binder Error（tick_cache TIMESTAMP 比较 CAST）与注入破坏的类结构（mypy 检出 100+ attr-defined → 归零）。**验收达成**：演化进程零长驻写连接、跨进程写 filelock 串行、写后连接即关、读路径 read_only。**遗留（2026-08-13 34 计划 47i 里程碑回归登记）**：`tests/test_duckdb_reader.py::test_read_while_writer_open` 与 read_only 读池设计冲突——写连接 `lock_configuration=true` + 读连接 `read_only=True` 配置不兼容同文件并存（`ConnectionException: Can't open a connection to same database file with a different configuration`）；该测试 docstring 描述旧「读池用普通连接」设计（L8-9），与 E.4 S1 read_only 改造冲突，待按 read_only 设计适配或登记降级。**遗留已解决（2026-08-13 核对）**：测试已改为 `test_read_after_writer_closed`——按 E.4 S1「写短生命周期」语义（写入即关，读池 `read_only=True` 短连接只在写关闭后打开），docstring 明确记录 DuckDB 配置约束与规避方式，实测写关闭后读可正常打开并读到最新数据；该遗留项关闭。P4 清理项（elite JSON 快照/状态 JSON/.npy/experiments 与旧 `data/state.duckdb`）仍受冻结期约束待执行。） |
| GAP-091 | `fts/data_futures_fundamental.py`（AkshareFuturesFundamentalProvider 预留接口）+ AKShare 仓单接口 | 期货仓单数据未接入：AKShare 仓单接口仅 CZCE（`futures_warehouse_receipt_czce`）/GFEX（`futures_gfex_warehouse_receipt`）可用，SHFE（`futures_shfe_warehouse_receipt`）/DCE（`futures_warehouse_receipt_dce`）因官网改版（SHFE `.dat` 永久 404）+ WAF 反爬（DCE 412，requests/curl_cffi 均被拦截）程序化不可用；且均为单日快照接口 | 库存/基差已接入（GAP-083 补充），仓单维度缺失影响全品种供需平衡信号（交易所仓单 vs 社会库存互补） | 2026-08-11 | ✅ 已关闭（**阶段 1+2 完成（2026-08-11）**：CZCE/GFEX 走官方接口——`VARIETY_EXCHANGE` 交易所路由 + ThreadPoolExecutor 并行逐日 + 跳过周末 + 25 自然日窗口（SR 实测 72,243 手）；SHFE/DCE/INE 改走东财 RPT_FUTU_STOCKDATA（阶段 2 新增 `EM_WAREHOUSE_MAP` + `_fetch_warehouse_receipt_em`，字段 ON_WARRANT_NUM→warehouse_receipt / ADDCHANGE→change，Choice 归一化注册仓单口径，历史近 3 个月；RB 实测 36,512 / M 25,100 / NR 16,632 / SC 2,961,000）；中金所股指无商品仓单降级空；data.py `enrich_futures_fundamental` 注入 fut_warehouse_receipt/fut_warehouse_receipt_chg；全品种闭环，测试 40 passed（新增东财路由/映射覆盖 5 例）） |
| GAP-092 | `fts/factor_engine/macro_regime.py`（GAP-092 新建）+ 宏观数据面板 | 宏观制度维度缺失（Bridgewater 增长×通胀四象限）：Regime 体系当前仅基于量价（HMM 后验/规则软投票），无宏观制度判定层 | 制度检测缺宏观维度，跨资产/跨周期 regime 判定失真（与头部机构差距，见 plans/28 §6） | 远期（P2） | ✅ 已关闭（v2.104.0+3：新建 `fts/factor_engine/macro_regime.py`——`MacroQuadrant` 枚举（overheat/goldilocks/stagflation/recession）+ `MacroRegimeDetector`（水平阈值判定：growth_score=clip((PMI−50)/5)、inflation_score=clip((CPI−2)/2)，得分≥0 归高侧；置信度=主象限联合软概率 p_g×p_i 独立假设，四象限概率和=1；`MacroRegimeConfig` 阈值/带宽/尺度可配；`QUADRANT_PROFILES` 象限画像含板块偏好；空/全 NaN 返回 None 不伪造）；数据基于已闭环宏观基础设施——通胀=CPI 当月同比（`EastmoneyMacroSource` 东财 RPT_ECONOMY_CPI，GAP-088 edb_cache）+ 增长=制造业 PMI（akshare `macro_china_pmi`「制造业-指数」，GAP-087 同款时序完整）；`scripts/macro_regime_report.py` 报告 CLI（--json/--trace-id/阈值可调，数据缺失如实标注 unavailable）；真实实测 2026-07：PMI 49.2 + CPI 0.5% → **recession 衰退** 置信 0.507（四象限概率 0.053/0.368/0.072/0.507）；月度发布天然滞后防未来函数，不改变量价 regime 链路（向后兼容，量价/宏观双维度独立并存）；测试 test_macro_regime 15 + test_macro_regime_report 4 全绿） |
| GAP-093 | `fts/factor_engine/regime*.py`（远期）+ RL 决策层 | RL 制度条件决策层缺失（SSRN 5785443）：制度判定→组合决策为规则/概率链，无强化学习条件决策层 | 制度→仓位/权重映射为静态规则，非数据驱动优化 | 远期（P2） | 🔴 开放（2026-08-11 plans/28 登记；需实盘反馈闭环，simulated_portfolio 已铺垫） |
| GAP-094 | `fts/factor_engine/regime_calibration.py`（远期） | 置信度 isotonic/Platt 概率校准缺失：confidence 为原始后验/规则伪概率，未经历史 regime 标签校准 | 置信度概率语义不严格（0.7 ≠ 70% 命中率），exposure_scale 缩放基准可能偏差 | 远期（P2） | ✅ 已关闭（v2.104.0+1：`StatisticalRegimeCalibrator` 统计概率校准——isotonic（保序回归，sklearn 惰性导入）/platt（逻辑回归 S 形映射）/binning（等频分箱+组内命中率，零依赖兜底）三方法，sklearn 缺失/拟合异常自动降级 binning；样本 <min_samples/NaN/标签非 0-1 拒绝拟合、predict 透传安全默认；save/load JSON 原子写持久化（load 以 numpy 重建映射，不依赖 sklearn）；`brier_score` 校准质量评估；离线拟合脚本 `scripts/fit_regime_calibration.py`（滚动检测重放生成 (confidence, hit) 样本——bull→正收益/bear→负收益/oscillate→低振幅/high_vol→高波动/low_vol→低波动方向预期，Brier 前后对比 + 置信度阶梯命中率诊断表，dry-run/原子落盘）；`_compute_exposure_scale` 新增 `calibration_path` 接线（`AdaptiveWeightConfig.calibration_path` 默认 ""=熵标定，文件有效时优先统计校准，失败回退不阻断）；真实 RB0 校准产物 `data/regime_calibration.json` 已生成——阶梯表 [0.6,0.8) 平均置信 0.679↔实际命中率 0.673，置信度具备频率语义；测试 test_regime_calibration +10 + test_portfolio_loop_adaptive +4 + test_fit_regime_calibration 5 全绿） |
| GAP-095 | `fts/factor_engine/portfolio_loop.py`（远期） | regime blend 幂次调节（blend_power）缺失：概率混合权重为线性加权，无幂次锐化/钝化控制 | 概率分布过平时倍率拉平，blend 区分度不足（见 plans/28 §5 风险表） | 远期（P2） | ✅ 已关闭（v2.104.0+1：`_power_normalize_probs` 幂次归一化（p_i'=p_i^power/Σ_j p_j^power，>1 锐化大概率制度/<1 钝化趋平，零概率 1e-12 兜底防除零，power≤0/1.0 原样返回）；`regime_adaptive_weight_adjustment` 新增 `blend_power` 参数（默认 1.0 线性，向后兼容）并在概率混合前应用；`AdaptiveWeightConfig.blend_power`（默认 1.0）+ Step 2.5 透传 `aconfig.get("blend_power", 1.0)`；测试 test_portfolio_loop_adaptive +7（幂次归一化单测/锐化/钝化/默认线性回归保护/端到端锐化权重）） |
| GAP-119 | `fts/cli.py` `_cmd_catalog_verify` | catalog verify JSON↔DuckDB 快照漂移：DuckDB 因子资产库（SSOT，E.3 迁移后权威）337 因子 vs JSON elite 快照仅 10 文件，`only_in_duckdb=327`，verify 判定不一致退出码 1；JSON 层 E.3 起冻结为只读快照不再随晋升更新，verify 的一致性契约（JSON↔DuckDB 全等）相对 DuckDB SSOT 架构过期 | `fts catalog verify` 恒失败，WorkFlow 端到端 s8 组合风控阶段被阻断（2026-08-14 真实端到端实测） | 1 月内 | ✅ 已关闭（v2.104.0+26：契约修正为 DuckDB SSOT 单向校验权威 + `--backfill` 幂等回填——`_cmd_catalog_verify` 新增 `_scan_json_snapshots`（扫描+损坏跳过）/`_backfill_json_snapshots`（从 DuckDB 回填缺失快照，tmp 原子写、不覆盖既有），真实回填 327 个 JSON 快照后 `fts catalog verify` ✅ 一致（337 交集 337，rc=0），WorkFlow s8 组合风控端到端解锁；新增 tests/test_catalog_verify.py 9 用例全绿） |
| GAP-120 | `scripts/futures_signal_pipeline.py` `_classify_delta_moves` + `_generate_trading_advice_report` + `_compute_composite_scores` | 信号增量（delta = 今日信号 − 昨日信号）有效性未验证：交易建议报告第 3/4/5 节增量规则（±0.02 加速/减速阈值、第 3 批加仓"次日增量未反转"、止损"增量反转减半仓"、连续 3 天信号衰减平仓）为启发式经验规则——① 阈值 0.02 无统计依据，未做 delta 分组（十分位）次日/未来 5 日收益单调性检验；② 增量与未来收益的截面/时序 IC 未计算，"加速延续/减速反转"预测力未量化；③ 无 A/B 对照（启用 vs 关闭增量规则组合的净值/换手/回撤对比）；④ 阈值未按 regime（bull/bear）与信号强度分层校准 | 增量规则若为噪声驱动（day-over-day 差分放大模型输出噪声）→ 第 3 批加仓/止损减半/衰减平仓产生无效换手与摩擦成本；若有效则固定阈值未必最优，损失置信度区分；规则预测力无从量化（与 GAP-060 多持有期 IC 同类统计严谨缺口） | 下个里程碑 | ✅ 已关闭（v2.105.0：新增 `scripts/validate_signal_delta.py` 落地验证方案五步——① 历史快照 `reports/futures/signal_scores_history.jsonl` 回放构建 delta 序列；② delta 十分位分组单调性检验 + 增量 IC（对齐 G4 ICIR 口径）；③ 增量动量检验（Factor Momentum 方法）；④ 与「无增量规则」基线组合 A/B 净值/换手/回撤对比；⑤ 验证集阈值扫描 ±0.01~0.05 + regime 分层校准；输出验证报告量化增量规则预测力（±0.02 阈值/次日增量未反转/增量反转减半/3 天衰减平仓规则或保留/或按验证结果调整阈值，噪声驱动规则剔除防无效换手）） |
| GAP-121 | `fts/factor_engine/evaluation_chain.py` + `fts/factor_engine/panel_vector.py`（plans/37） | 横截面评估性能瓶颈：`_cs_execute_factors` 逐品种执行 + `_cs_compute_ics` 逐日 spearmanr 循环（真实 149 品种 × 3062 日单候选 ~2s）；全矩阵化方案 `panel_vector` 已实现并接入开关（`cross_section_panel_vector`，默认关闭，IC 矩阵化生产实测 ~1.2x），但 2D 因子执行引擎（因子执行 + Series/reindex 构建，占生产耗时主导）未落地 | 演化吞吐受限，完整 9.5x 提速未兑现 | 下个里程碑 | ✅ 已关闭（2026-08-15 登记，plans/37：Phase 1 IC 矩阵化已完成——30 用例对照全绿、开关 on/off 评估产出一致；**Phase 2 Step 1 面板化执行引擎已完成**——`execute_factor_panel` 动态抽样验证 + 安全回退，真实期货面板受内部缺口限制实测 ~1x（诚实口径，见 plans/37 §4.5）；**Phase 2 Step 2 批 1 已完成（v2.104.0+54）**——feature_ops 7 个 P0 热算子（ts_product/ts_zscore/ts_min_max_diff/ts_cum_max/max_drawdown/ts_argmin/self_corr）改 sliding_window_view 内核 + ops_library 9 处伪 `apply(np.sqrt)` 直改，算子因子单候选 ~35s → 实测 11–525x，test_rolling_native 28 对照用例全绿；**批 2 已完成（v2.104.0+55）**——registry.ts_argmax/ts_decay_linear + seed_loader/seed_data 双模板 7 算子改自包含向量化（实测 4.8–96.8x），test_seed_ops_native 56 对照用例全绿；**批 3 已完成（v2.104.0+56）**——ops_library 真滚动回调 14 处（ts_cvar_95/99、CCI md、ts_aroon_up/down、ts_linear_trend_score、ts_amp/amp_ratio、cs_trim_mean_diff/gini/concentration/top_bottom_spread、ts_volume_concentration/cycle）+ regime_features._rolling_autocorr + gp_evolver 模板 ts_product 改 `feature_ops._rolling_apply_native` 通用内核（含 NaN 窗口保留 + inf→NaN 语义对齐 pandas），test_ops_native_batch3 48 对照用例全绿，实测 5.1–147x；**Phase 3 已完成（v2.104.0+57）**——`cross_section_panel_vector` 默认开启（走航/审计自动复用预对齐面板路径），切换前实测缺口面板（149×3000，8 品种内部缺口）on/off 产出一致 + 性能持平（operator 1.00x / code 1.05x），新增 test_default_panel_vector_enabled，顺带修复 GAP-121 引入的 test_g11/g4 mock 装配缺陷（缺 `_prior_evaluations`）；**plans/39 §11 回退（v2.104.0+58）**——缺口面板 2D 化改造完成（计数回溯窗 + 缺口感知滚动内核 + `_GapAwareFrame`，test_gap_panel_2d 39 用例全绿）但真实期货缺口面板（149×3062，98% 列内部缺口）算子因子面板化实测 **0.3x（<5x 门槛）**→ 按 §7 回退并登记豁免：**评估链信号构建摘除 `execute_factor_panel` 恒逐品种执行，仅 IC 计算保留全矩阵化（`compute_cs_ics_vectorized`）**，产出与旧路径逐位一致；`execute_factor_panel` 保留为独立模块（缺口感知滚动内核供独立调用方/对照基准复用）不参与主链路；新增 test_chain_operator_panel_fallback_per_symbol（面板化打桩抛错契约）→ test_panel_vector 32 用例全绿）；**2026-08-17 审计收尾：全部计划工作落地（IC 矩阵化 + 算子原生向量化批 1-3 + Phase 3 默认开启，settings.py `cross_section_panel_vector` 默认 true 与代码核验一致）；剩余「完整 9.5x 提速」按 plans/39 §11 真实缺口面板（149×3062，98% 列内部缺口）实测 0.3x <5x 门槛正式豁免——信号构建恒逐品种零漂移、仅 IC 矩阵化，`execute_factor_panel` 保留独立模块登记豁免不参与主链路**） |
| GAP-126 | `fts/factor_engine/extractors/futures_pipeline.py` | 提取器源注册表配置化（plans/41 B1）：源清单仍硬编码注册（tinysoft/broker_reports/academic_papers/macro_events/web_search），新增因子源需改代码；未下沉 `config/extractors.yaml` SSOT（enabled/paused/max_factors/params 每源声明） | 新增/启停因子源需改代码重启，扩展成本高 | 下个里程碑 | ✅ 已关闭（v2.105.0：源注册表下沉 `config/extractors.yaml` SSOT——每源声明 enabled/paused/max_factors/params，`futures_pipeline.py` 构造器改为从 YAML 加载源清单（替代硬编码注册 tinysoft/broker_reports/academic_papers/macro_events/web_search），新增因子源不再改代码、改 YAML 即生效，缺失/损坏配置回退内置默认） |
| GAP-127 | `fts/factor_engine/extractors/` | 量化平台 API 直连（plans/41 B2/C 远期）：聚宽/米筐/BigQuant 等平台因子库 API 多数闭源/需授权，未做逐平台适配；当前动态源仅 WebSearchExtractor（公开网页检索） | 平台独有因子数据（社区因子/回测结果）无法程序化接入 | 远期 | 🔴 开放（2026-08-16 登记，收益受平台开放度制约） |
| GAP-131 | `fts/factor_engine/meta_loop.py` + `factor_program.py` | L1 拒绝候选不可追溯：编译失败等硬失败候选仅在日志记录后即丢弃，代码不落盘（2026-08-16 能化链 L1 实测 `fut_carry_roll yield` / `fut_hei_ma` 两因子沙箱编译失败被拒后代码完全丢失），事后无法回溯修复；且 `fix_factor_code` 自动修复仅 5 种策略，LLM 高频语法瑕疵（&&/||、一元 !、^ 幂、条件行内赋值 =、行尾残留反斜杠）未覆盖 | 被拒候选成为一次性丢弃数据，编译失败类因子无修复通道；自动修复率受限 | 当日 | ✅ 已关闭（v2.104.0+82：① `MetaLoop` 新增 `rejected_dir`（默认由 inject_dir 派生 l1_injected→l1_rejected / l1_injected_energy→l1_rejected_energy，可显式覆盖）+ `_persist_rejected`——硬失败（编译失败/重复，含 bootstrap 具体编译错误）与软失败重写后仍未达标候选落盘 `{candidate_id}.json`（完整 code + `l1_rejection` 元数据：reasons/rejected_at/trace_id/market）；② `fix_factor_code` 新增 Strategy 6 全局修复（&&/||→and/or、一元 !→not 掩蔽 != 防误改、^→**、if/elif/while 条件行内赋值 =→==、行尾残留反斜杠去除 + 组合变换），触发条件覆盖真实错误消息（invalid syntax / cannot assign / unexpected character）；新增 test_factor_program +7、test_meta_loop +6；受影响回归全绿 + ruff/mypy 通过） |
| GAP-139 | `fts/factor_engine/l3_signal_service.py`（`load_or_build_signal_matrix`/`persist_signal_matrix`）+ `fts/factor_engine/portfolio_loop.py`（`_auto_build_factor_returns` end_date 推导） | D 层增量仅"同窗口因子级复用"（plans/51 B1 诚实记录）：增量判定维度 (factor, market, end_date) 且 end_date=面板最新交易日 → 每日窗口推进增量判定全 miss、A2 形状防护降级全量重算；plans/40 D2 承诺的"存量因子仅追加最近交易日窗口信号、不重算历史"未实现（正确性已由 A1 双哈希 + A2 形状防护保证，性能未兑现） | 每日 L3 组合重算仍全量重算全部因子信号，增量库仅同日多次运行/重试命中（跨日运行重算量=全量） | 下个里程碑 | ✅ 已关闭（v2.104.0+，plans/52 落地：① meta 表增 `dates_digest` 列（blake2 日期序列指纹，`_init_tables` 幂等迁移 + 存量库缺列降级全量安全兼容）；② `_classify_reusable` 前缀判定细分可复用因子（直读 / 增量窗口追加 / 降级全量）；③ `_append_window_signals` 增量执行——旧窗尾部 `_W_RECALL=500` 回退段 + 新增交易日切片执行 + 截断，`_verify_append` 抽样 2 品种对照验证兜底（不过自动全量零漂移）；④ `load_or_build_signal_matrix` 接线 + 单因子整窗回写 `_persist_factor_bundle`；⑤ 配置 `l3_signal_store_append_window`（默认 true，env 可关回退现行为）；新增测试 6 用例（增量 vs 全量逐位一致/rolling 窗口 50 回退/前缀不一致降级/验证失败兜底/旧库兼容）+ config 1 用例，test_l3_signal_service 26 passed + 受影响回归 400 passed + ruff/mypy 通过，详见 plans/52） |
| GAP-142 | `fts/factor_engine/energy_qa_review.py` | 能化链评审（l2_review_energy_job）与定期质检（scripts/energy_chain_degradation_dryrun.py）职责重叠、判据竞争（同一因子可能质检判 degraded 而评审判 retire），且各自重复准备面板与 reaudit | 合并为统一管道（宁严勿松单维度降级 + 冷却期30日自动回归 + 单一状态机），消除重复计算与判据冲突 | 灰度 4 周（dry-run 对比后 apply） | 🔄 处理中（v2.104.0+87；2026-08-17 审计：统一管道 EnergyQaReviewPipeline 已实施并接入 jobs.py `l2_energy_qa_review_job`，冷却期 30 交易日回归 + 单一状态机，灰度对比观察中） |
| GAP-148 | `fts/scheduler/jobs.py`（`_read_kline_cache`）+ `fts/monitor/data_level_monitor.py`（`check_missing`）+ `fts/cli.py`（`_build_default_aggregator`）+ `data/fts_history.duckdb`（kline_cache） | 双符号变体混行致监控误报 + 样本缩水 + 历史冗余：① 旧版 TDX 写入的无 0 后缀变体（SC/FU/BU 等 80 个、168,308 行，vwap/source/oi_change/fetched_at 全空）与当前 0 后缀变体日期完全重叠，`_read_kline_cache` 按 date 去重混行 → 近 120 日仅剩 63 交易日 + 全表缺失率虚高 31.7%（2026-08-19 实测）；② adj_factor 为 RollCalendar 读取时计算字段（缓存按设计不落库）100% 缺失被计入监控误报；③ sync 聚合器 `_build_default_aggregator` enhancers=[] 未注册 TQSDK 增强源，kline_cache 近 120 日 oi_change 全缺 | 数据级监控 critical 告警 14 条/日（31.7% 缺失率）淹没真实告警；监控样本窗口被腰斩；历史裸数据变体双写污染读路径 | 当日 | ✅ 已关闭（v2.105.0+17：① `_read_kline_cache` 查询窗口放大（limit×4）+ 字段完整度优先去重（同 date 保留 vwap/source/oi_change 等齐全行）+ 去重后截取最近 limit 交易日 + pre_settle 前日结算代理填充 → 样本恢复 120 交易日、vwap/source 缺失清零；② `check_missing` 豁免派生字段 adj_factor；③ `_build_default_aggregator` 注册 TQSDKEnhanceSource（对齐 FuturesDataProvider 构建模式，.env 已有凭据；本地 tqsdk×Python3.12 logging 兼容问题失败降级不阻断）；④ 数据治理：无 0 后缀历史变体 80 个 168,308 行备份 `data/archive/kline_cache_legacy_nosuffix_20260819.parquet` → 删除 0 后缀已覆盖日期冗余 149,493 行 + 迁移 0 后缀缺失历史日期 18,815 行到 0 后缀 → 无后缀变体清零、总行数 323,827→174,334；⑤ 实测：数据级监控 critical 14→0、全表缺失率 31.7%→5.9%（剩余 oi_change 为真实数据缺口，待 TQSDK 环境修复后由增强源补全）；新增测试 3 用例（双符号完整度去重/adj_factor 派生豁免/sync 增强源注册），test_data_level_monitor 25→28 全绿） |
| GAP-152 | `fts/data_sources/tqsdk_tick_source.py` + `data/fts_history.duckdb`（kline_cache.hold） | hold（持仓量）真实数据源缺口：kline_cache hold 字段 100% 缺失（GAP-148 已注册 TQSDKEnhanceSource 待生效），当前走 20 日滚动均量代理——代理为显式降级（GAP-151 已告警 + 数据级监控 proxy_missing_ratio 量化）但非真实持仓 | hold 依赖因子使用代理值，无真实持仓数据的日子「合理但失真」 | 外部依赖（TQSDK 环境/权限） | 🔴 开放（2026-08-19 登记，P2：TQSDK 环境可用后验证 hold 覆盖，缺失率经 proxy_missing_ratio 指标可观测下降） |
| GAP-153 | `fts/factor_engine/` 因子消费链路 + `config/futures_field_consumption.py`（consumers 字段） | hold 依赖因子无消费侧数据充足性门禁：代理率超阈值（GAP-151 proxy_ratio_critical）时，依赖 hold 的因子仍正常参与计算/组合，失真无消费侧拦截 | 代理失真传导至因子信号与组合权重 | 排期（P2） | 🔴 开放（2026-08-19 登记，P2：用字段消费字典 consumers 对 hold 依赖因子做数据充足性门禁/质量标记） |
| GAP-154 | `fts/data_sources/tqsdk_source.py` `_SYMBOL_MAP` + `fts/data_sources/tqsdk_enhance_source.py` | TQSDK 增强源品种映射缺 IM0（中证1000）：CFFEX 六品种（IF0/IH0/IC0/IM0/TF0/TS0）唯一缺 IM0 → `_resolve_symbol` 报"未知品种映射"直传原值，天勤无法解析 → IM0 hold/oi_change 字段增强静默失效（2026-08-19 Beta 层巡检实测 `[TQSDK_ENHANCE] 未知品种映射: IM0`） | IM0 持仓量无法经 TQSDK 真实源补全（hold 走代理降级，与 GAP-152 缺口叠加） | 当日 | ✅ 已关闭（v2.105.0+28：`_SYMBOL_MAP` 增 `"IM0": "KQ.m@CFFEX.IM"`，实测增强层 IM0 取数恢复（3 行）；测试 +1（TestResolveSymbol.test_im0_cffex_mapping），test_tqsdk_enhance_source+test_tqsdk_source 34 passed 全绿） |
| GAP-155 | `fts/monitor/logic_monitor.py`（`check_market_premise`）+ `fts/factor_engine/regime.py`（`high_vol_premise_check`）+ `fts/factor_engine/portfolio_loop.py`（Step 2.5） | high_vol 制度标签与市场前提监控口径分歧（plans/54 P0-3 落地补盲）：① 组合 high_vol 标签（RegimeAwareSelector multi_hmm）与规则法 vol 维度（EWMA≥q80）及前提监控（等权指数 20d 波动分位）三重背离，2026-08-19 实测 HMM conf 0.99 vs 规则法非高波 vs premise 分位 0.45；② `check_market_premise` 分位基于传入面板全历史未固定窗口，180d/252d/437d 窗口结果 0.45/0.57/0.71 漂移，告警阈值边界不稳（180d 误触发 / 252d 不触发） | 高波标签与真实波动结构背离时权重调制与降权决策失真；前提告警窗口敏感存在误报/漏报边界不稳 | 当日 | ✅ 已关闭（v2.105.0+30：① `check_market_premise` 新增 `vol_window=252` 默认参数固定分位窗口（对齐 regime.py `_VOL_HISTORY_DAYS`）；② `regime.py` 新增 `high_vol_premise_check`——规则法 vol 前提交叉验证（EWMA≥q80 或 20d 波动分位≥0.5 任一成立即前提有效，数据不足不误报，返回 ok/ewma_vol/eff_high/vol_percentile/reason）；③ portfolio_loop Step 2.5 接线（仅 energy）：high_vol 前提不成立 → 标签覆盖 oscillate + conf×0.6 + method 标注 premise_override，regime_meta 新增 `premise_cross_check` 段透明留痕；新增测试 7 用例（test_market_premise +2 窗口固定 + test_regime_premise_check.py 5），受影响回归全绿） |
| GAP-156 | `fts/data_sources/quantdata_provider.py`（缺失）+ `fts/core/enums.py` + `fts/data_sources/aggregator.py` | FTS 与 QuantData 权威数据源零接入：docs/harness/01-architecture.md 曾声称 v2.105.0+11 "QuantData 集成已实现"，但全项目 grep `QuantData` 仅命中该文档（代码零引用），`quantdata_provider.py` 从未创建，`DataSource` 枚举无 QUANTDATA 成员，aggregator 降级链无 QuantData 源——FTS 实际仍消费本地 kline_cache（TQ-Local/TqSdk/AKShare 多源） | 权威性原则无落地载体，因子消费数据来源权威性受质疑 | 本次实施（主链路切换） | 🔴 开放（2026-08-19 登记，随 QuantData 主链路切换计划实施） |
| GAP-157 | `D:\QuantData`（无 fundamental 表）+ `fts/data_futures_fundamental.py` + `fts/config/futures_field_consumption.py`（fundamental 9 字段） | 库存/仓单/现货基差无权威数据源：QuantData 实测无 fundamental/basis 表（kline_daily 仅 OHLCV+OI）；FTS 现有 fundamental 9 字段（fut_inventory/fut_warehouse_receipt/fut_spot_price/fut_near_basis 等）全部来自 AKShare/东财/WebSearch（非权威，权威性受质疑），且 `enrich_futures_fundamental` 无调用点（死代码） | fundamental 类因子与结构约束只能基于非权威数据，违背权威性原则 | 待 QuantData 侧补数据源 | 🔴 开放（2026-08-19 登记，字段权威矩阵 L2 禁依赖；需 QuantData 补齐库存/仓单/基差采集后接线，本次不启用） |
| GAP-158 | `D:\QuantData\market_data\kline_history.duckdb`（kline_daily 表）+ `fts/data_futures.py`（get_ohlcv 9 列契约） | settle/amount/vwap/pre_settle 无权威源：QuantData kline_daily 仅 OHLCV+open_interest（无 amount/settle），`client_v2.get_factor_input` 对缺失列填 NaN；FTS 现有 settle/amount 来自 TQ-Local/AKShare 增强（非权威） | 依赖 settle 的因子（基差近似类）与 amount 类字段只能走非权威降级/代理，权威性不可保证 | 持续（L1 降级层） | 🟡 部分（2026-08-19 登记：字段权威矩阵 L1 降级·非权威，标注来源不硬拒；QuantData 侧补 settle 采集后可升 L0） |
| GAP-159 | `fts/data_futures.py`（`_init_default_aggregator`）+ `fts/cli.py`（`_build_default_aggregator`） | 天勤源默认挂载违背 QuantData 主链路解耦原则（plans/57）：主链路为 QUANTDATA 时仍默认注册 TQSDKEnhanceSource/TQSDKSource(5m)/TQSDKTickSource，aggregator.get_ohlcv 命中缓存后无条件执行 `_enhance_fields` → L1/L2 感知链路逐品种自建天勤连接（.env 免费账号连 free-api）+ 15s wait_update，L1 Meta-Loop 实测每品种 ~20s（2026-08-20 实测 30 品种感知被拖慢）；QuantData 下 hold 已为 L0 权威（open_interest），天勤增强冗余且产生外部网络依赖 | 感知链路性能拖慢 + FTS 对数据采集方式非无感；外部数据源异常（天勤限流/断网）波及因子感知 | 当日 | ✅ 已关闭（v3.0.0+2：新增 `tqsdk_sources_enabled` 配置（env `FTS_TQSDK_SOURCES_ENABLED`，默认 false）统一门控三处挂载点——`_init_default_aggregator`（enhancers 天勤增强/minutes 天勤 5m/ticks 天勤 tick）与 `cli._build_default_aggregator`（enhancers 天勤增强）；默认不挂载 → 感知链路零天勤连接；显式 `true` 恢复旧行为；`futures_enhance_enabled` 语义收敛为「已启用天勤增强源基础上追加 IFindSDKSource」；新增 test_data_futures 挂载开关 2 用例 + test_cli 1 用例） |
| GAP-160 | `fts/factor_engine/evaluation_chain.py`（`cross_section_evaluate_backtest`）+ `fts/factor_engine/audit.py`（`_check_cross_symbol`）+ `fts/factor_engine/symbol_holdout.py` | 演化审计链跨品种验证不适配全期货异构品种池（2026-08-20 实测）：① `symbol_holdout` 在训练池 19 品种内部按行业分层留出 5 品种验证（n_holdout=5 单期截面 IC 噪声大 + 留出行业构成失衡），属样本内验证，与 GAP-075"防记忆池子"初衷部分背离，且与 `cross_symbol`（同为训练池 19 品种）信息重叠——盲测池 FUTURES_HOLDOUT 15 品种（与训练池零重叠、覆盖 10 条产业链）在 fts/factor_engine/ 零引用从未进入审计链；② `cross_symbol` 主防线 0.8/二项检验临界 14/19（73.7%）对 7 大板块异构品种池过严，板块特异因子（仅 5/7 板块有效）被系统性误杀（6 日累计 143 次 symbol_holdout + 40 次 cross_symbol 淘汰，fct_be39c34b 16/19 品种正 IC 仅因 retention=0.486<0.5 差 1.4pp 被边界误杀） | 因子多样性受损（板块特异因子无法入库）+ 样本内验证统计效力差 + 真外延泛化（盲测池）未被审计 | v3.0.0+7 | ✅ 已关闭（v3.0.0+7：① `cross_section_evaluate_backtest` 新增 `holdout_panel_data` 参数——`symbol_holdout` 默认接入盲测池 FUTURES_HOLDOUT 15 品种（逐品种时序 IC 均值=holdout_ic，不要求共同窗口规避晚上市品种；`SymbolHoldoutConfig.use_blind_pool=true`，false 回退训练池内留出），retention=holdout_ic/\|train_ic\| ≥ 0.5 且 holdout_ic>0 通过；② `cross_symbol` 新增第 4 通道「板块级覆盖率」（`FactorAuditConfig.min_sector_coverage=5`）——训练池 7 大板块中 ≥5 板块有代表品种 IC 为正即通过，与 A+C 三通道 OR；③ 演化主链（evolution_loop/evolution_seeds/evolution_futures）自动构建盲测 panel（FTSDataProvider + FUTURES_HOLDOUT，失败容错 None 回退旧路径）；新增 test_audit_sector_coverage.py（板块覆盖率通道 + 盲测池模式 + 回退兼容） |
| GAP-167 | `fts/factor_engine/qa/pre_entry.py`（Q3）+ `qa/quarterly_check.py`（F3）+ `qa/monthly_check.py` | 参数稳健性仅离散档位（plans/59 OPT-07）：Q3/F3 只要求 ≥3 组离散参数档位 + 季度查偏移（>1 档标记），参数平面"连续可行域"未检测，窄峰参数因子可能蒙混过关 | 参数鲁棒性判定粒度粗，最优参数区间记录与跨期漂移监控有空窗 | 3 月内 | ✅ 已关闭（2026-08-20 v3.0.0+24 plans/59 OPT-07 实施：新增 `qa/param_robustness.py`（`param_perturbations` 数值参数 ±20% 三档网格 + `compute_param_robustness` 鲁棒区占比（衰减 ≤ 阈值网格点占比，浮点容差）+ `robust_ratio_verdict`）→ 三接入点：① Q3 入库质检（build_qa_review）评估链产出 `param_robustness` 时按其 verdict 判定（fragile 窄峰参数一票否决，缺失回退 params 非空向后兼容）；② F3 季度复检新增 `param_robust_ratio` 优先鲁棒区判定（<0.60 窄峰参数标记，缺失回退档位偏移）；③ 月度复检新增 `param_robust_ratio` 附加预警（不进 M1-M5 warn_count 不改变处置梯度）；默认开启（`FTS_PARAM_ROBUST_ENABLED`）；新增 test_param_robustness.py 19 用例 + qa/evolution_promote 回归 125 用例全绿 + ruff 通过） |
| GAP-168 | `fts/factor_engine/factor_quality_card.py`（capacity_score/tradability_score）+ `fts/config/factor_quality_card_config.py` + `scripts/liquidity_snapshot.py` | 成本/容量静态失真（plans/59 OPT-08）：capacity/turnover 评分静态估算，期货合约月份/换月窗口流动性变化大，移仓期容量分与实际可交易容量偏离 | 换月期容量/交易性评分失真，组合构建与实际流动性错配 | 3 月内 | ✅ 已关闭（2026-08-20 v3.0.0+25 plans/59 OPT-08 实施：新增 `qa/liquidity_env.py`（`liquidity_env_scale`——移仓窗口 → scale_min 0.5 / 价差比超警告线线性下调 / 快照缺失不误伤；`apply_capacity_scale` clamp [0,5]）→ `FactorQualityCard.evaluate` 新增 `liquidity_scale` 参数（默认 1.0 向后兼容），capacity_score/tradability_score 计算后按系数下调，其余 8 维度不受影响；调用方按 `scripts/liquidity_snapshot.py` 聚合快照（roll_active/spread_ratio）传入；默认开启（`FTS_LIQUIDITY_ENV_ENABLED`）；新增 test_liquidity_env.py 13 用例 + qa/quality_card 回归 241 用例全绿 + ruff 通过——plans/59 八项优化全部完成） |
| GAP-174 | `fts/factor_engine/scope_domain/`（P2，v3.0.0+14） | 品种级特异因子无真伪鉴别：单品种高 IC 无法与过拟合噪声区分，现有全链口径误伤品种特异 | 品种特异与噪声混同，特异因子价值无法释放或被误杀 | P2 处理中 | 🔄 部分关闭（v3.0.0+14：`evaluate_symbol_scope` 品种域评估 + `symbol_scope_guard` 真伪护栏（样本窗≥500/跨子期≥0.67/显著性）已交付，Q10 品种级分支护栏通过才判"品种特异"；**剩余**：演化生成端无品种级通道，护栏仅在评估/评审/Q10 消费，真实品种特异挖掘依赖后续生成端改造） |
| GAP-175 | `fts/factor_engine/scope_domain/specific_fields.py` + `config/specific_fields.yaml` + `scripts/collect_specific_fields.py`（P3+P3.5，v3.0.0+15~17） | 品种特有数据源缺失：fundamental 9 字段为全品种统一 schema，无法表达品种特有逻辑（特定交割库/船运贴水/产业政策） | 品种特异因子的数据前提缺失 | P3 框架先行 | 🔄 部分关闭（v3.0.0+15：注册表 + 加载器 + `symbol_focus` 已交付；v3.0.0+16：真实 parquet 加载通道——`enrich_specific_fields` date 对齐注入，SC0.sc_freight_premium 首个启用字段；v3.0.0+17：**采集脚本骨架**——`scripts/collect_specific_fields.py`（manual csv/json 导入→parquet 幂等 upsert / placeholder 真实源接入报告位，dry-run 支持）；**剩余**：真实行情源接入（SC0 贴水需外部授权数据源，placeholder 位待扩展）） |

### GAP-C 系列 — Stage 3C 远期机构级差距（细则：plans/23-institutional-transformation-plan.md §3.3）

> 承接总纲 23 号计划 §1 差距矩阵残留 🟡/🔴 单元格（Stage 3C 远期项）。C1/C2/C3/C5/C6/C7/C8 已于 2026-08-11 首期实施；C4 同日实施（单机 LocalCluster 代码/测试/吞吐基准落地），真实多机集群部署待硬件/基建条件成熟后按 DaskBackend 抽象接入（记录见 07-operations 与 plans/23 C4 实施设计）。

| ID | 模块 | 差距描述 | 影响 | 处理期限 | 状态 |
|:---|:-----|:---------|:-----|:---------|:-----|
| C1 | `fts/factor_engine/microstructure_generator.py` + `fts/factor_engine/evolution_loop.py`（2026-08-11 首期 + 评估晋升接线） | Level2 微观结构因子接入 L2 演化并入 elite | 微观结构 alpha 未进入 elite 池（Stage 3 退出标准②） | Stage 3C（2026-08-11 首期） | ✅ 已实施（首期：生成器 tick→日频聚合→FactorProgram + CLI micro-generate + 执行器 datetime 对齐通道 + 20 用例；**评估晋升接线 2026-08-11**：`run_microstructure_promotion`（横截面评估 ic≥0.03&sharpe≥1.5 → FactorAuditor 6 项审计 → `_promote_to_elite` 护栏）+ CLI micro-evaluate + 7 用例；真实评估待 tick 数据积累） |
| C2 | `fts/factor_engine/alternative_sentiment.py`（2026-08-11 首期 + LLM 精修） | 另类数据因子上线（舆情 NLP，公开新闻 API + 词典法） | 舆情/事件维度缺失（Stage 3 退出标准③） | Stage 3C（2026-08-11 首期） | ✅ 已实施（首期：FinancialSentimentLexicon 词典打分 + EastmoneyNewsProvider + 日聚合 sent_mean/sent_std/sent_chg + CLI senti-generate + 26 用例；**LLM 精修 2026-08-11**：`LlmSentimentScorer` + `evaluate_lexicon_consistency` 词典-LLM 一致性 ≥0.7 验收接口 + CLI senti-consistency；评估晋升接线对齐 C1 模式待数据积累） |
| C3 | `fts/factor_engine/black_litterman.py`（2026-08-11 新增） | ML 组合层：Black-Litterman 观点融合权重 | 组合层无观点融合类 ML 权重（Stage 3 退出标准④） | Stage 3C（2026-08-11 首期） | ✅ 已实施（BL 闭式/auto-views/CLI bl 模式 + 22 用例） |
| C4 | `fts/factor_engine/executor_backend.py`（GAP-I502 扩展） | 多节点分布式挖掘工厂（Dask 集群实战部署） | 单机吞吐上限（Stage 3 退出标准①） | Stage 3C（2026-08-11 首期） | 🟡 已实施首期/二期多机后置开放（2026-08-11：DaskBackend 增强——cluster 句柄注入/address 优先/worker_count 调度器视角诊断/kill_worker 故障注入/单任务异常隔离迭代器；`BatchMiner.filter_batch` executor_backend="dask" 接线；pyproject 新增 `distributed` extra 并入 all；`scripts/benchmark_executor.py` 吞吐基准（thread/process/dask LocalCluster 对比表）；17 用例全绿 + 既有执行器/batch_mining 回归通过；真实多机集群部署后置，按 DaskBackend(address="tcp://scheduler:8786") 接入） |
| C5 | `fts/ml/models.py` + `fts/ml/deep_factor.py`（2026-08-11 首期） | 深度模型：轻量 Transformer 因子（GAN 合成远期） | 缺深度时序特征维度 | Stage 3C（2026-08-11 首期：轻量 Transformer） | ✅ 已实施（纯 numpy 因果注意力/零未来函数 + L2 接线 + 21 用例；GAN 远期） |
| C6 | `fts/factor_engine/recalibration.py`（2026-08-11 新增） | 在线学习与自动重校准（实盘反馈驱动参数微调） | 无在线增量重校准（仅告警/退役） | Stage 3C（2026-08-11 首期） | ✅ 已实施（重校准队列/elite 回写/触发源接线 + 18 用例） |
| C7 | `fts/factor_engine/cost_model.py` + `scripts/calibrate_impact_cost.py`（2026-08-11） | 回测保真实证化（冲击成本实证标定 + 融资成本） | 冲击成本参数人工设定、无融资成本项 | Stage 3C（2026-08-11 首期） | ✅ 已实施（FTS_COST_* 配置化/融资成本/标定脚本 + 20 用例） |
| C8 | `fts/monitor/http_server.py` + `fts/factor_engine/expr_dsl/registry.py` + `fts/factor_engine/feature_ops.py`（2026-08-11 首期 + C9 扩容二期） | 基础设施深化（在线人审协作工作台 + 算子库扩容至数百） | 无 Web 工作台、算子规模距数百有差 | Stage 3C（2026-08-11 首期） | ✅ 已实施（人审工作台 /review + /api/review/* 复用 FactorReviewWorkflow + C8 22 算子双注册表扩容（DSL 102）+ operator_catalog 自动生成 + 浏览器冒烟走通；C8-2 2026-08-11 机审/人审可配置：ReviewMode（FTS_REVIEW_MODE 默认 auto）+ AutoReviewPolicy 三态判定（正常批准/低质驳回/异常转人审）+ auto_review 批量机审 reviewer=auto + CLI/Web 触发 + 28 用例；**C9 扩容二期 2026-08-11**：30 算子双注册表扩容（DSL 102→**132**、GP 81→**111**、verify consistent/mismatched 0）+ test_operator_expansion_c9 39 用例；5 个异常因子模拟审批 test_simulated_approval 9 用例；算子库距数百仍剩远期扩容量） |

---

## 4. 优先级定义

| 优先级 | 定义 | 处理时限 | 验证标准 |
|:-------|:-----|:---------|:---------|
| **P0** | 阻塞性问题，影响核心功能的正确性和可靠性 | 1 周内 | 新增测试覆盖率达到 80%+，相关模块无 P0 bug |
| **P1** | 重要改进，提升系统效率或稳定性 | 1 月内 | 新增测试覆盖率达到 70%+，关键路径全覆盖 |
| **P2** | 一般改进，优化代码质量和可维护性 | 3 月内 | 新增测试覆盖率达到 50%+ |

---

## 5. 差距关闭流程

1. 编写测试代码并通过 PR 审查
2. 运行完整测试套件确认全部通过（2928+ passed, 0 failed）
3. 更新本文件中的差距状态
4. 更新 `06-testing.md` 中的覆盖统计
5. 如果涉及架构变更，更新 `01-architecture.md`

---

## 一致性元数据

| 字段 | 值 |
|:-----|:----|
| 代码→文档映射 | 本文件登记 **79 项差距**（P0 2 + P1 40 + P2 29 + GAP-C 8），覆盖 `fts/factor_engine/`（regime_crowding/regime_beta_layer/regime_thresholds/regime_profile/regime_gate/regime_validation/macro_regime/scope_domain/symbol_holdout/subchain_*/evaluation_chain/evolution_promote/evolution_loop/factor_inspector/qa/factor_db/factor_program/numba_kernels/meta_loop/l3_signal_service/panel_vector 等）、`fts/data_sources/`（tqsdk_*/quantdata_provider/aggregator/data_futures/data_fundamental）、`fts/scheduler/jobs.py`、`fts/monitor/`（logic_monitor/data_level_monitor/factor_inspector/data_quality_monitor）、`fts/store/`、`fts/cli.py`、`fts/config/`、`scripts/`（futures_signal_pipeline/validate_signal_delta/collect_specific_fields 等）与 `config/specific_fields.yaml` 等模块。 |
| 可验证断言 | **开放 26 / 已关闭 53**（P0 0 / 2；P1 11 / 29；P2 14 / 15；GAP-C 1 / 7）。开放口径：状态列非「✅」项（🔴 开放 GAP-093/127/132/152/153/156/157/161~168/170/173 + 🟡 部分 GAP-089/090/150/151/158 + 🔄 处理中 GAP-142/174/175 + C4）；关闭口径：状态列为「✅/已实施/已修复」且关闭版本 ≥ v2.104.0（GAP-081/091/092/094/095/114~121/123/125/126/128~131/133~140/144/145/147~149/152/154/155/159/160/169/171/172/I306/I307 + C1/C2/C3/C5/C6/C7/C8）。2026-08-20 全量瘦身校正：删除 v2.104.0 前关闭的早期条目（GAP-001~113、GAP-X01~X03、GAP-I 系列 20 项、GAP-L 系列 10 项）与第 3 节差距详情；P1 表 GAP-154~158 重编号为 GAP-169~173，原 L 系列 GAP-161/162 重编号为 GAP-174/175（详见文件头校正说明）。 |
| 检验方式 | 检查本文件差距登记表确认所有差距状态列标注准确（✅ 已关闭/🔴 开放/🟡 部分/🔄 处理中）；总数与总览表口径一致；编号无重复；关闭记录关联 plans/* 计划文档与 07-operations.md 版本历史。 |
